---
phase: quick-260630-f75
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [apps-script/Código.js]
autonomous: true
requirements: [F75-FECHA-NORM]
must_haves:
  truths:
    - "Una fecha tecleada como texto dd/mm/yyyy en columnas de fecha se normaliza a yyyy-MM-dd"
    - "Las celdas Date y los strings yyyy-MM-dd ya correctos siguen devolviendo ISO sin cambios"
    - "Las columnas que no son de fecha (nombre, email, ids, presente, activo, etc.) no se tocan"
  artifacts:
    - path: "apps-script/Código.js"
      provides: "Constante DATE_COLUMNS + helper normalizeSheetDate_ + sheetToObjects con normalizacion de fechas"
      contains: "function normalizeSheetDate_"
  key_links:
    - from: "sheetToObjects"
      to: "normalizeSheetDate_"
      via: "DATE_COLUMNS.indexOf(header) !== -1"
      pattern: "normalizeSheetDate_\\(val, tz\\)"
---

<objective>
Normalizar fechas no-ISO en las columnas de fecha dentro de `sheetToObjects`. Si Aurora teclea una fecha como TEXTO en formato `dd/mm/yyyy` (locale espanol) en columnas de fecha, hoy queda como string crudo y rompe las comparaciones lexicas downstream (filtro de convocatoria activa en `handleGetConvocatorias`/`warmCache`, ventanas por fecha en `computeResumen`).

Purpose: Cero errores silenciosos por entrada manual de fechas — la convocatoria activa y el resumen dejan de fallar cuando una celda de fecha viene como `dd/mm/yyyy` en vez de `yyyy-MM-dd` o como objeto Date.
Output: Un solo commit `fix:` en espanol que toca exclusivamente `apps-script/Código.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Estado actual del archivo. El executor usa estas referencias directas — sin exploracion adicional. -->

Branch actual: `fix/cache-invalidacion-y-activo-robusto` (NO main). Verificar con `git branch --show-current` antes de tocar nada.

apps-script/Código.js — fragmentos relevantes (lineas exactas verificadas):

Lineas 14-20 (constante a nivel de modulo, en seccion CONFIGURACION):
```javascript
const SHEET_NAMES = {
  CONVOCATORIAS: 'CONVOCATORIAS',
  PROFESORES: 'PROFESORES',
  ALUMNOS: 'ALUMNOS',
  ASISTENCIA: 'ASISTENCIA',
  LOG: 'LOG'
};
```

Linea 114 — inicio de seccion UTILIDADES (banner de comentarios `// ===`). `sheetToObjects` es la primera funcion de esta seccion.

Lineas 118-149 — `sheetToObjects` (firma y retorno NO se tocan):
```javascript
/**
 * Convierte una hoja en array de objetos usando la fila 1 como cabeceras.
 */
function sheetToObjects(sheetName) {
  const ss = SpreadsheetApp.getActiveSpreadsheet();
  const sheet = ss.getSheetByName(sheetName);
  if (!sheet) return [];

  const data = sheet.getDataRange().getValues();
  if (data.length < 2) return [];

  const headers = data[0].map(h => h.toString().trim());
  const rows = [];

  for (let i = 1; i < data.length; i++) {
    const row = data[i];
    // Saltar filas vacias (primera columna vacia)
    if (!row[0] && row[0] !== false && row[0] !== 0) continue;

    const obj = {};
    headers.forEach((header, j) => {
      let val = row[j];
      // Convertir fechas de Google Sheets a string ISO
      if (val instanceof Date) {
        val = Utilities.formatDate(val, Session.getScriptTimeZone(), 'yyyy-MM-dd');
      }
      obj[header] = val;
    });
    rows.push(obj);
  }
  return rows;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: Normalizar fechas dd/mm/yyyy texto a ISO en sheetToObjects</name>
  <files>apps-script/Código.js</files>
  <action>
Tres ediciones puntuales en `apps-script/Código.js`, todas dentro o cerca de la seccion UTILIDADES. NO cambiar la firma ni el retorno de `sheetToObjects` (sigue devolviendo array de objetos). NO normalizar columnas que no sean de fecha.

1. Definir la constante `DATE_COLUMNS` a nivel de modulo: `const DATE_COLUMNS = ['fecha', 'fecha_inicio', 'fecha_fin'];`. Colocarla cerca de `SHEET_NAMES` (lineas 14-20) en CONFIGURACION, o al inicio de UTILIDADES (linea 114). Estas son las cabeceras de columna que reciben tratamiento de fecha.

2. Añadir el helper `normalizeSheetDate_(v, tz)` en la seccion UTILIDADES (antes o despues de `sheetToObjects`), con JSDoc en espanol. Logica:
   - Si `v instanceof Date` → devolver `Utilities.formatDate(v, tz, 'yyyy-MM-dd')`.
   - Si `typeof v === 'string'`: `const s = v.trim();`
     - Si `s` matchea `/^\d{4}-\d{2}-\d{2}$/` → devolver `s` (ya es ISO correcto).
     - Si `s` matchea `/^(\d{1,2})\/(\d{1,2})\/(\d{4})$/` → interpretar los grupos como DIA/MES/AÑO (locale espanol), normalizar dia y mes a 2 digitos con padding ('0' inicial si len<2) y devolver `año + '-' + mes + '-' + dia`.
     - Cualquier otro string → devolver `s` tal cual (NO romper: la guarda downstream ya lo maneja).
   - Cualquier otro tipo → devolver `v` sin tocar.
   - El JSDoc DEBE documentar la asuncion clave: el formato `dd/mm/yyyy` se interpreta como DIA/MES/AÑO (locale espanol), no mes/dia.

3. Dentro de `sheetToObjects`, calcular `tz` UNA sola vez antes del bucle de filas (justo despues de `const rows = [];`, ej: `const tz = Session.getScriptTimeZone();`). Reemplazar el bloque de conversion dentro de `headers.forEach` (lineas 140-143 actuales) por:
   - Si `DATE_COLUMNS.indexOf(header) !== -1` → `val = normalizeSheetDate_(val, tz);`
   - Si NO (else) → mantener el comportamiento actual: si `val instanceof Date` → `val = Utilities.formatDate(val, tz, 'yyyy-MM-dd');` (para columnas como `hora_registro`, igual que hoy).
   - El resto de columnas string quedan intactas.

CRITICO — no romper nada: el comportamiento para celdas Date y para strings `yyyy-MM-dd` ya correctos NO cambia (siguen dando ISO). Solo se AÑADE el manejo de `dd/mm/yyyy` texto en columnas de fecha. No tocar `hora_registro` salvo el Date→ISO ya existente. Idioma: comentarios y JSDoc en espanol, codigo (nombres) en ingles.

UN solo commit `fix:` en espanol, solo `apps-script/Código.js`. Verificar primero que el branch activo es `fix/cache-invalidacion-y-activo-robusto` (NO main).
  </action>
  <verify>
    <automated>node --check "apps-script/Código.js" && grep -q "function normalizeSheetDate_" "apps-script/Código.js" && grep -q "const DATE_COLUMNS" "apps-script/Código.js" && grep -q "normalizeSheetDate_(val, tz)" "apps-script/Código.js" && grep -Eq "val instanceof Date" "apps-script/Código.js"</automated>
  </verify>
  <done>
`node --check` pasa sin errores de sintaxis. Existe `function normalizeSheetDate_`. `DATE_COLUMNS` esta definido como constante de modulo. `sheetToObjects` llama a `normalizeSheetDate_(val, tz)` dentro del `headers.forEach`. La rama `val instanceof Date` sigue presente (no se rompio la conversion ISO de columnas no-fecha como `hora_registro`). Un solo commit `fix:` en espanol que toca exclusivamente `apps-script/Código.js`.
  </done>
</task>

</tasks>

<verification>
- `node --check "apps-script/Código.js"` sin errores.
- `grep "function normalizeSheetDate_"` → 1 coincidencia.
- `grep "const DATE_COLUMNS"` → 1 coincidencia.
- `grep "normalizeSheetDate_(val, tz)"` → al menos 1 coincidencia (uso dentro de sheetToObjects).
- `grep "val instanceof Date"` → sigue presente (rama de columnas no-fecha intacta).
- `git diff --name-only` lista UNICAMENTE `apps-script/Código.js`.
</verification>

<success_criteria>
- Una celda de fecha tecleada como `dd/mm/yyyy` (ej: `30/06/2026`) en columna `fecha`, `fecha_inicio` o `fecha_fin` se devuelve como `2026-06-30`.
- Una celda Date sigue devolviendose como `yyyy-MM-dd` (sin cambios).
- Un string ya en `yyyy-MM-dd` se devuelve igual (sin cambios).
- Columnas no-fecha (nombre, email, ids, presente, activo, justificada, motivo) no se alteran; `hora_registro` Date→ISO sigue como hoy.
- La firma y el retorno de `sheetToObjects` no cambian.
- Un solo commit `fix:` en espanol, exclusivamente sobre `apps-script/Código.js`.
</success_criteria>

<output>
Crear `.planning/quick/260630-f75-fix-normaliza-fechas-dd-mm-yyyy-a-iso-en/260630-f75-SUMMARY.md` al terminar.
</output>
