---
phase: 04-documentacion-y-accesibilidad
plan: 04
type: execute
wave: 1
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - src/components/features/StudentRow.jsx
  - src/components/ui/Modal.jsx
  - src/pages/DashboardPage.jsx
  - src/components/features/ConvocatoriaSelector.jsx
autonomous: true
requirements: [DOCS-01, DOCS-02, A11Y-01, A11Y-02, A11Y-03, A11Y-04]
gap_closure: true

must_haves:
  truths:
    - "`npm run lint` pasa con 0 errores (9 errores jsx-a11y actuales eliminados)"
    - "StudentRow es operable por teclado (convertido a button semantico)"
    - "Resultado de busqueda en DashboardPage es operable por teclado (convertido a button semantico)"
    - "Label de ConvocatoriaSelector esta asociado al select via htmlFor/id"
    - "Modal overlay y dialog no generan errores de lint (eslint-disable con justificacion)"
  artifacts:
    - path: "src/components/features/StudentRow.jsx"
      provides: "Fila de alumno como <button> semantico con estilos equivalentes"
      contains: "<button"
    - path: "src/components/ui/Modal.jsx"
      provides: "eslint-disable en overlay y dialog con justificacion documentada"
      contains: "eslint-disable-next-line"
    - path: "src/pages/DashboardPage.jsx"
      provides: "Resultado de busqueda como <button> semantico"
      contains: "<button"
    - path: "src/components/features/ConvocatoriaSelector.jsx"
      provides: "label asociado a select via htmlFor + id"
      contains: "htmlFor"
  key_links:
    - from: "src/components/features/StudentRow.jsx"
      to: "npm run lint"
      via: "button semantico elimina click-events-have-key-events + no-static-element-interactions"
      pattern: "<button"
    - from: "src/components/ui/Modal.jsx"
      to: "npm run lint"
      via: "eslint-disable-next-line suprime errores con justificacion documentada"
      pattern: "eslint-disable-next-line jsx-a11y"
    - from: "src/pages/DashboardPage.jsx"
      to: "npm run lint"
      via: "button semantico elimina click-events-have-key-events + no-static-element-interactions"
      pattern: "<button"
    - from: "src/components/features/ConvocatoriaSelector.jsx"
      to: "npm run lint"
      via: "htmlFor en label vinculado a id en select"
      pattern: "htmlFor"
---

<objective>
Cerrar los 9 errores jsx-a11y restantes en 4 archivos para que `npm run lint` pase con 0 errores.

Purpose: El criterio de exito 4 de Phase 4 ("npm run lint pasa sin errores con eslint-plugin-jsdoc y eslint-plugin-jsx-a11y activados") fallo en verificacion con 9 errores activos. Este plan cierra esos gaps.

Output: Los 4 archivos corregidos, `npm run lint` con 0 errores (warnings tolerados).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-documentacion-y-accesibilidad/04-VERIFICATION.md

<interfaces>
<!-- Patron eslint-disable ya establecido en TeacherCard.jsx (Plan 03): -->
<!-- {/* eslint-disable-next-line jsx-a11y/click-events-have-key-events, jsx-a11y/no-static-element-interactions -- D-02: contenido expandido es informativo, no interactivo por teclado */} -->

<!-- Patron AlertList boton semantico (Plan 02): -->
<!-- <button onClick={...} className="w-full text-left ..."> reemplaza <div onClick={...}> -->

<!-- useFocusTrap ya maneja Escape a nivel document (src/hooks/useFocusTrap.js): -->
<!-- El overlay onClick={onClose} en Modal es una affordance solo de mouse; Escape ya esta cubierto -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convertir StudentRow y DashboardPage search results a button semanticos + eslint-disable en Modal + htmlFor en ConvocatoriaSelector</name>
  <files>
    src/components/features/StudentRow.jsx,
    src/pages/DashboardPage.jsx,
    src/components/ui/Modal.jsx,
    src/components/features/ConvocatoriaSelector.jsx
  </files>
  <read_first>
    src/components/features/StudentRow.jsx,
    src/pages/DashboardPage.jsx,
    src/components/ui/Modal.jsx,
    src/components/features/ConvocatoriaSelector.jsx,
    src/hooks/useFocusTrap.js,
    src/components/features/AlertList.jsx
  </read_first>
  <action>
Corregir los 9 errores jsx-a11y en los 4 archivos. Cada correccion descrita a continuacion:

**1. StudentRow.jsx (2 errores: click-events-have-key-events + no-static-element-interactions en linea 24)**

Convertir el `<div>` raiz (linea 24) a `<button>`. Patron identico al usado en AlertList (Plan 02). Cambios concretos:

- Cambiar `<div` a `<button type="button"` en linea 24
- Cambiar `</div>` de cierre correspondiente (linea 58) a `</button>`
- Agregar `type="button"` para evitar form submission accidental
- Agregar clase Tailwind `text-left w-full` para reset de estilos de button nativo (el button por defecto centra texto)
- Eliminar `cursor-pointer` (ya implicito en button nativo)
- MANTENER el `onClick={onToggle}` existente
- MANTENER el `style={{ animationDelay }}` existente (es una excepcion documentada en CLAUDE.md Gotchas)
- MANTENER todas las clases Tailwind de borde, fondo, etc. existentes
- NO agregar `role="button"` (ya es un button nativo, seria redundante)

El className resultante del button debe ser:
```
'animate-slide-up flex items-center gap-3 p-3 mb-2 rounded-xl text-left w-full transition-all duration-300 border-[1.5px] ' + (isPresent ? 'bg-burgundy-soft border-burgundy/20' : 'bg-white border-border-light')
```

**2. DashboardPage.jsx (2 errores: click-events-have-key-events + no-static-element-interactions en linea 82)**

Convertir el `<div>` de cada resultado de busqueda (linea 82) a `<button>`. Cambios concretos:

- Cambiar `<div` (linea 82) a `<button type="button"`
- Cambiar el `</div>` de cierre correspondiente (linea 93) a `</button>`
- Agregar `type="button"` para evitar form submission accidental
- Agregar clases `text-left w-full` para reset de estilos de button
- Eliminar `cursor-pointer` del className
- MANTENER `key={student.id}` y el `onClick` handler existente
- MANTENER todas las demas clases Tailwind (bg-white, border, rounded, hover, etc.)

El className resultante del button debe ser:
```
"bg-white border border-border rounded-[10px] py-2.5 px-3 mb-1.5 text-left w-full transition-all duration-200 hover:bg-burgundy-soft"
```

**3. Modal.jsx (4 errores en lineas 26 y 30)**

Agregar comentarios `eslint-disable-next-line` con justificacion. NO convertir a button porque:
- El overlay (linea 26) es un `<div>` con `onClick={onClose}` para cierre por click fuera. La equivalencia de teclado (Escape) ya esta cubierta por `useFocusTrap`. No es un elemento interactivo — es una affordance de mouse complementaria al teclado.
- El dialog (linea 30) tiene `role="dialog"` (no-interactivo) con `onClick={e => e.stopPropagation()}` que es solo prevencion de propagacion, no interactividad.

Cambios concretos:

Antes de la linea 26 (el overlay div), agregar:
```
{/* eslint-disable-next-line jsx-a11y/click-events-have-key-events, jsx-a11y/no-static-element-interactions -- Overlay: cierre por click fuera; equivalencia teclado (Escape) cubierta por useFocusTrap */}
```

Antes de la linea 30 (el dialog div), agregar:
```
{/* eslint-disable-next-line jsx-a11y/click-events-have-key-events, jsx-a11y/no-noninteractive-element-interactions -- Dialog: stopPropagation no es interactividad; role="dialog" es no-interactivo con teclado via useFocusTrap */}
```

NOTA: El segundo error en linea 30 es `no-noninteractive-element-interactions` (NO `no-static-element-interactions`) porque `role="dialog"` hace que el elemento sea no-interactivo con nombre. La regla exacta que aparece en `npm run lint` es `jsx-a11y/no-noninteractive-element-interactions`.

**4. ConvocatoriaSelector.jsx (1 error: label-has-associated-control en linea 14)**

Asociar el `<label>` con el `<select>` via `htmlFor` + `id`. Cambios concretos:

- En el `<label>` (linea 14): agregar `htmlFor="conv-selector"`
- En el `<select>` (linea 17): agregar `id="conv-selector"`

El label resultante:
```jsx
<label htmlFor="conv-selector" className="font-montserrat text-[11px] text-white/50 block mb-1.5">
```

El select resultante:
```jsx
<select
  id="conv-selector"
  value={selectedId || ''}
  onChange={(e) => { ... }}
  className="..."
>
```
  </action>
  <verify>
    <automated>cd C:/Users/Usuario/Desktop/novattend && npx eslint --no-fix 2>&1 | grep -c "error" | grep -q "0" && echo "PASS: 0 errors" || (npx eslint --no-fix 2>&1; echo "FAIL: errors remain")</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` reporta 0 errores (warnings tolerados)
    - `grep "<button" src/components/features/StudentRow.jsx` encuentra al menos 1 match
    - `grep "type=\"button\"" src/components/features/StudentRow.jsx` encuentra match
    - `grep "text-left" src/components/features/StudentRow.jsx` encuentra match
    - `grep "<button" src/pages/DashboardPage.jsx` encuentra al menos 1 match (el resultado de busqueda)
    - `grep "type=\"button\"" src/pages/DashboardPage.jsx` encuentra match
    - `grep "eslint-disable-next-line" src/components/ui/Modal.jsx` encuentra 2 matches (overlay + dialog)
    - `grep "useFocusTrap" src/components/ui/Modal.jsx` sigue presente (no se rompio el import)
    - `grep "htmlFor" src/components/features/ConvocatoriaSelector.jsx` encuentra match
    - `grep "id=\"conv-selector\"" src/components/features/ConvocatoriaSelector.jsx` encuentra match
    - `npm test` sigue pasando (no se rompieron tests existentes)
  </acceptance_criteria>
  <done>
    Los 9 errores jsx-a11y eliminados: StudentRow y DashboardPage search results convertidos a button semanticos, Modal con eslint-disable justificado, ConvocatoriaSelector con label asociado. `npm run lint` pasa con 0 errores.
  </done>
</task>

</tasks>

<verification>
1. `npm run lint` pasa con 0 errores — criterio de exito 4 de Phase 4 satisfecho
2. `npm test` sigue pasando — no se rompieron tests existentes
3. Los 4 archivos modificados mantienen menos de 250 lineas (regla CLAUDE.md)
4. No se introdujeron estilos inline nuevos (regla CLAUDE.md)
</verification>

<success_criteria>
- `npm run lint` reporta exactamente 0 errores (los 5 warnings de jsdoc son tolerados)
- Los 4 archivos modificados compilable y funcional (npm run build pasa)
- StudentRow y search results en DashboardPage son buttons HTML nativos operables por teclado
- Modal mantiene su comportamiento existente (focus trap + Escape) sin cambios funcionales
- ConvocatoriaSelector label esta programaticamente asociado al select
</success_criteria>

<output>
After completion, create `.planning/phases/04-documentacion-y-accesibilidad/04-04-SUMMARY.md`
</output>
