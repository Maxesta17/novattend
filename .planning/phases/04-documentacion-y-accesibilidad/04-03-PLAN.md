---
phase: 04-documentacion-y-accesibilidad
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/components/features/GroupTabs.jsx
  - src/pages/AttendancePage.jsx
  - src/components/features/TeacherCard.jsx
autonomous: true
requirements: [A11Y-01, A11Y-02]
must_haves:
  truths:
    - "CEO puede expandir/colapsar TeacherCard con Enter/Space y colapsar con Escape usando solo teclado"
    - "GroupTabs responde a Arrow Left/Right moviendo foco entre tabs, y Tab sale del grupo"
    - "GroupTabs tiene roles tablist/tab con aria-selected correcto"
    - "El panel de alumnos en AttendancePage tiene role=tabpanel con aria-labelledby"
  artifacts:
    - path: "src/components/features/GroupTabs.jsx"
      provides: "WAI-ARIA Tabs completo con keyboard navigation"
      contains: "role=\"tablist\""
    - path: "src/pages/AttendancePage.jsx"
      provides: "role=tabpanel wrapper con aria-labelledby"
      contains: "role=\"tabpanel\""
    - path: "src/components/features/TeacherCard.jsx"
      provides: "role=button + aria-expanded + keyboard handler"
      contains: "role=\"button\""
  key_links:
    - from: "src/components/features/GroupTabs.jsx"
      to: "src/pages/AttendancePage.jsx"
      via: "id=tab-grupo-{g} linked by aria-labelledby on tabpanel"
      pattern: "tab-grupo-"
    - from: "src/components/features/TeacherCard.jsx"
      to: "onToggle prop"
      via: "handleKeyDown dispatches onToggle on Enter/Space"
      pattern: "handleKeyDown"
    - from: "src/components/features/GroupTabs.jsx"
      to: "onChange prop"
      via: "ArrowLeft/ArrowRight dispatch onChange + focus management"
      pattern: "ArrowRight|ArrowLeft"
---

<objective>
Implementar accesibilidad compleja por teclado: WAI-ARIA Tabs en GroupTabs con navegacion por flechas, tabpanel en AttendancePage, y keyboard handler completo en TeacherCard header.

Purpose: Estos son los dos cambios de accesibilidad mas complejos de Phase 4. GroupTabs requiere el patron WAI-ARIA Tabs completo (roles, tabIndex management, arrow key navigation, focus management). TeacherCard requiere un div con role=button interactivo por teclado. Ambos son requisitos criticos para WCAG 2.1 Nivel A.

Output: GroupTabs y TeacherCard completamente operables por teclado, con ARIA correcto, y `npm run lint` pasando para estos archivos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-documentacion-y-accesibilidad/04-CONTEXT.md
@.planning/phases/04-documentacion-y-accesibilidad/04-RESEARCH.md
@.planning/phases/04-documentacion-y-accesibilidad/04-UI-SPEC.md

<interfaces>
<!-- Full current source of both files — executor needs exact structure -->

From src/components/features/GroupTabs.jsx (30 lines — COMPLETE):
```jsx
/**
 * Pestanas de seleccion de grupo.
 * @param {object} props
 * @param {number[]} props.groups - Lista de numeros de grupo (ej: [1,2,3,4])
 * @param {number} props.selected - Grupo actualmente seleccionado
 * @param {function} props.onChange - Handler al cambiar grupo (recibe numero)
 */
export default function GroupTabs({ groups, selected, onChange }) {
  return (
    <div className="flex gap-1.5">
      {groups.map(g => (
        <button
          key={g}
          onClick={() => onChange(g)}
          className={[
            'flex-1 py-2 rounded-lg border-none cursor-pointer',
            'font-cinzel text-[13px] font-semibold',
            'transition-all duration-300',
            selected === g
              ? 'bg-burgundy text-gold'
              : 'bg-white/5 text-white/[0.38]',
          ].join(' ')}
        >
          Grupo {g}
        </button>
      ))}
    </div>
  )
}
```

From src/components/features/TeacherCard.jsx (143 lines — key sections):
```jsx
// Line 14: export default memo(function TeacherCard({ teacher, isExpanded, onToggle, onStudentClick })
// Lines 29-50: Header div with onClick={onToggle} — NO role, NO tabIndex, NO aria-expanded
// Line 49: <ChevronIcon rotated={isExpanded} /> — NO aria-hidden
// Lines 72-127: GroupSection — private helper with onClick divs (D-02: NOT keyboard-navigable)
// Lines 129-143: ChevronIcon — private helper SVG (needs aria-hidden="true")
```

From src/pages/AttendancePage.jsx (line 15):
```jsx
export default function AttendancePage() {
  // Uses GroupTabs at some point
  // Student list rendered below GroupTabs — needs role="tabpanel" wrapper
}
```

Pattern reference from src/hooks/useFocusTrap.js:
- Shows how to manage refs and keyboard handlers in this codebase

Pattern reference from src/components/ui/Modal.jsx:
- Shows ARIA attribute patterns (role, aria-modal, aria-label)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WAI-ARIA Tabs en GroupTabs + tabpanel en AttendancePage</name>
  <files>src/components/features/GroupTabs.jsx, src/pages/AttendancePage.jsx</files>
  <read_first>
    - src/components/features/GroupTabs.jsx (30 lineas — reescritura completa con ARIA)
    - src/pages/AttendancePage.jsx (completo — localizar donde se renderiza GroupTabs y la lista de alumnos debajo)
    - src/hooks/useFocusTrap.js (referencia de patron useRef para focus management)
  </read_first>
  <action>
    **GroupTabs.jsx** — Reescribir con WAI-ARIA Tabs completo (per D-04, D-05, D-06):

    ```jsx
    import { useRef, useCallback } from 'react'

    /**
     * Pestanas de seleccion de grupo con patron WAI-ARIA Tabs.
     * Navegacion: Arrow Left/Right mueve entre tabs, Tab sale del grupo.
     * @param {object} props
     * @param {number[]} props.groups - Lista de numeros de grupo (ej: [1,2,3,4])
     * @param {number} props.selected - Grupo actualmente seleccionado
     * @param {function} props.onChange - Handler al cambiar grupo (recibe numero)
     */
    export default function GroupTabs({ groups, selected, onChange }) {
      const tabRefs = useRef({})

      const handleKeyDown = useCallback((e) => {
        const currentIdx = groups.indexOf(selected)
        let nextGroup = null

        if (e.key === 'ArrowRight') {
          e.preventDefault()
          nextGroup = groups[(currentIdx + 1) % groups.length]
        } else if (e.key === 'ArrowLeft') {
          e.preventDefault()
          nextGroup = groups[(currentIdx - 1 + groups.length) % groups.length]
        }

        if (nextGroup !== null) {
          onChange(nextGroup)
          // Mover foco al nuevo tab activo
          tabRefs.current[nextGroup]?.focus()
        }
      }, [groups, selected, onChange])

      return (
        <div role="tablist" aria-label="Grupos" onKeyDown={handleKeyDown} className="flex gap-1.5">
          {groups.map(g => (
            <button
              key={g}
              ref={(el) => { tabRefs.current[g] = el }}
              id={`tab-grupo-${g}`}
              role="tab"
              aria-selected={selected === g}
              tabIndex={selected === g ? 0 : -1}
              onClick={() => onChange(g)}
              className={[
                'flex-1 py-2 rounded-lg border-none cursor-pointer',
                'font-cinzel text-[13px] font-semibold',
                'transition-all duration-300',
                selected === g
                  ? 'bg-burgundy text-gold'
                  : 'bg-white/5 text-white/[0.38]',
              ].join(' ')}
            >
              Grupo {g}
            </button>
          ))}
        </div>
      )
    }
    ```

    Puntos criticos:
    - `tabRefs` usa `useRef({})` con callback refs para crear un mapa `{ groupNumber: element }`
    - `tabIndex`: solo el tab activo tiene `0`, los demas `-1` (Tab sale del grupo, no recorre todos)
    - `handleKeyDown` en el contenedor tablist — Arrow Left/Right con wrap circular y `.focus()` inmediato
    - `e.preventDefault()` obligatorio en Arrow keys para evitar scroll
    - `id="tab-grupo-${g}"` necesario para el `aria-labelledby` del tabpanel en AttendancePage

    **AttendancePage.jsx** — Agregar `role="tabpanel"` y `aria-labelledby` al contenedor de la lista de alumnos. Localizar donde se renderiza la lista de StudentRow despues de GroupTabs. Envolver esa seccion con un div:

    ```jsx
    <div
      role="tabpanel"
      aria-labelledby={`tab-grupo-${selectedGroup}`}
    >
      {/* ... lista de StudentRow existente ... */}
    </div>
    ```

    Si la lista de alumnos ya esta envuelta en un `<div>`, agregar los atributos al div existente. Si esta directamente en el flow, agregar un wrapper div nuevo.

    La variable que contiene el grupo seleccionado se llama `selectedGroup` en AttendancePage (verificar leyendo el archivo). El id debe coincidir con `tab-grupo-{g}` de GroupTabs.
  </action>
  <verify>
    <automated>cd C:/Users/Usuario/Desktop/novattend && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - GroupTabs.jsx contiene `role="tablist"`
    - GroupTabs.jsx contiene `aria-label="Grupos"`
    - GroupTabs.jsx contiene `role="tab"`
    - GroupTabs.jsx contiene `aria-selected={selected === g}`
    - GroupTabs.jsx contiene `tabIndex={selected === g ? 0 : -1}`
    - GroupTabs.jsx contiene `id={`tab-grupo-${g}`}`
    - GroupTabs.jsx contiene `import { useRef, useCallback } from 'react'`
    - GroupTabs.jsx contiene `ArrowRight` y `ArrowLeft` en el handler
    - GroupTabs.jsx contiene `.focus()` para mover foco al tab activo
    - AttendancePage.jsx contiene `role="tabpanel"`
    - AttendancePage.jsx contiene `aria-labelledby={`tab-grupo-${selectedGroup}`}`
    - npm test pasa sin regresiones
  </acceptance_criteria>
  <done>GroupTabs implementa WAI-ARIA Tabs completo: tablist/tab roles, aria-selected, tabIndex management, arrow key navigation con focus. AttendancePage tiene tabpanel con aria-labelledby vinculado al tab activo.</done>
</task>

<task type="auto">
  <name>Task 2: Keyboard handler en TeacherCard + aria-hidden en ChevronIcon + lint fixes</name>
  <files>src/components/features/TeacherCard.jsx</files>
  <read_first>
    - src/components/features/TeacherCard.jsx (143 lineas — completo)
    - src/components/ui/Modal.jsx (referencia: patron ARIA role + aria-modal)
    - .planning/phases/04-documentacion-y-accesibilidad/04-CONTEXT.md (D-01, D-02, D-03 — decisiones bloqueadas)
  </read_first>
  <action>
    Tres cambios en TeacherCard.jsx (per D-01, D-02, D-03):

    **1. Keyboard handler en la funcion principal TeacherCard:**

    Agregar `handleKeyDown` dentro de la funcion TeacherCard, DESPUES de `const toggleGroup = ...` y ANTES del `return`:

    ```jsx
    const handleKeyDown = (e) => {
      if (e.key === 'Enter' || e.key === ' ') {
        e.preventDefault()
        onToggle()
      } else if (e.key === 'Escape' && isExpanded) {
        e.preventDefault()
        onToggle()
      }
    }
    ```

    **2. Atributos ARIA en el div de la cabecera (lineas 29-50):**

    Cambiar el `<div onClick={onToggle}>` a:
    ```jsx
    <div
      role="button"
      tabIndex={0}
      aria-expanded={isExpanded}
      onClick={onToggle}
      onKeyDown={handleKeyDown}
      className="bg-white border-[1.5px] border-border rounded-xl p-3 flex items-center gap-3 cursor-pointer transition-all duration-300 hover:bg-cream"
    >
    ```

    Los atributos nuevos son: `role="button"`, `tabIndex={0}`, `aria-expanded={isExpanded}`, `onKeyDown={handleKeyDown}`.
    El className NO cambia.

    **3. aria-hidden en ChevronIcon:**

    Modificar la funcion privada `ChevronIcon` (linea 129) para agregar `aria-hidden="true"` al SVG:
    ```jsx
    function ChevronIcon({ rotated, size = 16 }) {
      return (
        <svg
          aria-hidden="true"
          className={`transition-transform duration-300 ${rotated ? 'rotate-180' : ''}`}
          width={size}
          height={size}
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          strokeWidth="2"
        >
          <polyline points="6 9 12 15 18 9" className="text-text-muted" />
        </svg>
      )
    }
    ```

    **4. ESLint overrides para GroupSection y student divs (D-02):**

    Los divs clickables dentro de `GroupSection` (linea 80-81: div onClick para grupo, linea 102-105: div onClick para estudiante) son intencionalmente NO navegables por teclado (D-02: contenido expandido es informacion visual del CEO). jsx-a11y marcara errores `click-events-have-key-events` y `no-static-element-interactions`.

    Agregar `eslint-disable-next-line` en las 2 ubicaciones:

    Para el div del grupo (linea ~80):
    ```jsx
    {/* eslint-disable-next-line jsx-a11y/click-events-have-key-events, jsx-a11y/no-static-element-interactions -- D-02: contenido expandido es informativo, no interactivo por teclado */}
    <div
      onClick={onToggle}
      className="flex items-center gap-2.5 py-2.5 px-3 ..."
    >
    ```

    Para el div del estudiante (linea ~102):
    ```jsx
    {/* eslint-disable-next-line jsx-a11y/click-events-have-key-events, jsx-a11y/no-static-element-interactions -- D-02: contenido expandido es informativo, no interactivo por teclado */}
    <div
      key={student.id}
      onClick={() => onStudentClick({...})}
      className="flex items-center gap-2 ..."
    >
    ```

    El comentario `-- D-02:` referencia la decision del usuario para trazabilidad.

    **5. Verificar lint final:**
    Ejecutar `npm run lint` y confirmar que TeacherCard.jsx no tiene errores (warnings aceptables de jsdoc en GroupSection y ChevronIcon — son helpers privados, exentos por `publicOnly: true`).
  </action>
  <verify>
    <automated>cd C:/Users/Usuario/Desktop/novattend && npx vitest run 2>&1 | tail -5 && npm run lint 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - TeacherCard.jsx contiene `role="button"` en el div de la cabecera
    - TeacherCard.jsx contiene `tabIndex={0}` en el div de la cabecera
    - TeacherCard.jsx contiene `aria-expanded={isExpanded}` en el div de la cabecera
    - TeacherCard.jsx contiene `onKeyDown={handleKeyDown}` en el div de la cabecera
    - TeacherCard.jsx contiene `const handleKeyDown = (e) =>` con Enter, Space y Escape
    - TeacherCard.jsx contiene `e.key === 'Escape' && isExpanded`
    - ChevronIcon SVG contiene `aria-hidden="true"`
    - TeacherCard.jsx contiene al menos 2 instancias de `eslint-disable-next-line jsx-a11y/click-events-have-key-events`
    - Cada eslint-disable contiene `-- D-02:` en el comentario
    - npm test pasa sin regresiones
    - npm run lint pasa con 0 errores en TeacherCard.jsx
  </acceptance_criteria>
  <done>TeacherCard header operable por teclado: Tab para focus, Enter/Space para toggle, Escape para colapsar. ChevronIcon tiene aria-hidden. GroupSection/student divs tienen eslint-disable documentado por decision D-02. npm run lint pasa para este archivo.</done>
</task>

</tasks>

<verification>
1. `npm test` pasa sin regresiones
2. `npm run lint` pasa con 0 errores
3. GroupTabs tiene `role="tablist"`, tabs con `role="tab"` + `aria-selected` + `tabIndex` management
4. GroupTabs responde a ArrowLeft/ArrowRight con wrap circular y focus management
5. AttendancePage tiene `role="tabpanel"` con `aria-labelledby` correcto
6. TeacherCard header tiene `role="button"`, `tabIndex={0}`, `aria-expanded`, `onKeyDown`
7. TeacherCard responde a Enter/Space (toggle) y Escape (colapsar)
8. ChevronIcon tiene `aria-hidden="true"`
9. GroupSection/student divs tienen eslint-disable con referencia a D-02
</verification>

<success_criteria>
- CEO puede operar TeacherCard expandible usando solo Tab, Enter/Space y Escape sin mouse
- GroupTabs implementa WAI-ARIA Tabs completo con navegacion por flechas
- El panel de alumnos tiene tabpanel con aria-labelledby vinculado al tab activo
- npm run lint pasa con 0 errores en todos los archivos modificados
- npm test pasa sin failures
</success_criteria>

<output>
After completion, create `.planning/phases/04-documentacion-y-accesibilidad/04-03-SUMMARY.md`
</output>
