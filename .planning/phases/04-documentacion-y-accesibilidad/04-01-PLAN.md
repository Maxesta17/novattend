---
phase: 04-documentacion-y-accesibilidad
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.css
  - src/components/ui/ToggleSwitch.jsx
  - eslint.config.js
  - package.json
  - src/pages/AttendancePage.jsx
  - src/pages/DashboardPage.jsx
  - src/pages/LoginPage.jsx
  - src/pages/SavedPage.jsx
autonomous: true
requirements: [A11Y-03, DOCS-01, DOCS-02]
must_haves:
  truths:
    - "Todos los elementos interactivos muestran un focus ring burgundy de 2px al navegar con Tab"
    - "npm run lint pasa con 0 errores incluyendo eslint-plugin-jsx-a11y y eslint-plugin-jsdoc"
    - "Las 4 pages (Attendance, Dashboard, Login, Saved) tienen cabecera JSDoc con @returns"
  artifacts:
    - path: "src/index.css"
      provides: "Focus ring global :focus-visible en @layer base"
      contains: ":focus-visible"
    - path: "eslint.config.js"
      provides: "ESLint flat config con jsx-a11y recommended + jsdoc require-jsdoc"
      contains: "jsxA11y"
    - path: "src/pages/AttendancePage.jsx"
      provides: "Cabecera JSDoc"
      contains: "@returns {JSX.Element}"
    - path: "src/pages/DashboardPage.jsx"
      provides: "Cabecera JSDoc"
      contains: "@returns {JSX.Element}"
    - path: "src/pages/LoginPage.jsx"
      provides: "Cabecera JSDoc"
      contains: "@returns {JSX.Element}"
    - path: "src/pages/SavedPage.jsx"
      provides: "Cabecera JSDoc"
      contains: "@returns {JSX.Element}"
  key_links:
    - from: "eslint.config.js"
      to: "eslint-plugin-jsx-a11y"
      via: "jsxA11y.flatConfigs.recommended in extends array"
      pattern: "jsxA11y\\.flatConfigs\\.recommended"
    - from: "eslint.config.js"
      to: "eslint-plugin-jsdoc"
      via: "plugins: { jsdoc } + rules jsdoc/require-jsdoc"
      pattern: "jsdoc/require-jsdoc"
    - from: "src/index.css"
      to: "ToggleSwitch.jsx"
      via: "Global focus ring replaces custom focus-visible classes"
      pattern: "@layer base"
---

<objective>
Establecer la infraestructura base de Phase 4: focus ring global CSS, ESLint plugins (jsx-a11y + jsdoc), y JSDoc en las 4 pages que faltan.

Purpose: Sin esto, los planes 02 y 03 no pueden verificar su trabajo con lint ni visualizar focus rings. Esta es la base sobre la que se construye todo el trabajo de accesibilidad y documentacion.

Output: `src/index.css` con `:focus-visible` global, `eslint.config.js` con 2 plugins nuevos, 4 pages con JSDoc, y `npm run lint` pasando con 0 errores.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-documentacion-y-accesibilidad/04-CONTEXT.md
@.planning/phases/04-documentacion-y-accesibilidad/04-RESEARCH.md

<interfaces>
<!-- Current state of files to modify. Executor reads full files via read_first. -->

From eslint.config.js (current — 29 lines):
```javascript
import js from '@eslint/js'
import globals from 'globals'
import reactHooks from 'eslint-plugin-react-hooks'
import reactRefresh from 'eslint-plugin-react-refresh'
import { defineConfig, globalIgnores } from 'eslint/config'

export default defineConfig([
  globalIgnores(['dist', 'apps-script']),
  {
    files: ['**/*.{js,jsx}'],
    extends: [
      js.configs.recommended,
      reactHooks.configs.flat.recommended,
      reactRefresh.configs.vite,
    ],
    languageOptions: { ... },
    rules: {
      'no-unused-vars': ['error', { varsIgnorePattern: '^[A-Z_]' }],
    },
  },
])
```

From src/index.css (current — 4 lines):
```css
@tailwind base;
@tailwind components;
@tailwind utilities;
```

From src/components/ui/ToggleSwitch.jsx (focus-visible classes to remove, line 23):
```jsx
'focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-burgundy',
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Focus ring global + limpiar ToggleSwitch</name>
  <files>src/index.css, src/components/ui/ToggleSwitch.jsx</files>
  <read_first>
    - src/index.css (estado actual: 3 directivas Tailwind, 4 lineas)
    - src/components/ui/ToggleSwitch.jsx (36 lineas — eliminar clases focus-visible custom en linea 23)
  </read_first>
  <action>
    1. En `src/index.css`, agregar el bloque focus ring global DESPUES de `@tailwind utilities;`:

    ```css
    @layer base {
      :focus-visible {
        outline: 2px solid #800000;
        outline-offset: 2px;
      }
    }
    ```

    El hex `#800000` es equivalente al token `burgundy` — es CSS puro en `@layer base`, no una clase Tailwind, asi que es aceptable por CLAUDE.md (per D-07).

    2. En `src/components/ui/ToggleSwitch.jsx`, eliminar la linea completa que contiene las clases focus-visible custom:
    ```
    'focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-burgundy',
    ```
    Esta linea es el tercer elemento del array de clases (linea 23). El ring global la reemplaza con output visual identico (per D-08).

    El array de clases debe quedar asi:
    ```jsx
    className={[
      'relative w-12 h-7 rounded-full transition-all duration-200 cursor-pointer border-none',
      checked
        ? 'bg-burgundy shadow-sm'
        : 'bg-disabled',
      className,
    ].filter(Boolean).join(' ')}
    ```
  </action>
  <verify>
    <automated>cd C:/Users/Usuario/Desktop/novattend && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/index.css contiene la cadena exacta `outline: 2px solid #800000;`
    - src/index.css contiene la cadena exacta `outline-offset: 2px;`
    - src/index.css contiene `@layer base`
    - src/components/ui/ToggleSwitch.jsx NO contiene `focus-visible:outline`
    - src/components/ui/ToggleSwitch.jsx sigue conteniendo `role="switch"` y `aria-checked` (no se rompio nada)
    - npm test pasa sin regresiones
  </acceptance_criteria>
  <done>Focus ring burgundy global activo en :focus-visible para todos los elementos interactivos. ToggleSwitch usa el ring global sin clases custom duplicadas.</done>
</task>

<task type="auto">
  <name>Task 2: Instalar ESLint plugins y configurar eslint.config.js</name>
  <files>package.json, eslint.config.js</files>
  <read_first>
    - eslint.config.js (estado actual: 29 lineas con 3 plugins existentes)
    - package.json (para confirmar devDependencies existentes)
  </read_first>
  <action>
    1. Instalar las dos dependencias:
    ```bash
    npm install --save-dev eslint-plugin-jsx-a11y eslint-plugin-jsdoc
    ```

    2. Reescribir `eslint.config.js` con el siguiente contenido exacto:
    ```javascript
    import js from '@eslint/js'
    import globals from 'globals'
    import reactHooks from 'eslint-plugin-react-hooks'
    import reactRefresh from 'eslint-plugin-react-refresh'
    import jsxA11y from 'eslint-plugin-jsx-a11y'
    import jsdoc from 'eslint-plugin-jsdoc'
    import { defineConfig, globalIgnores } from 'eslint/config'

    export default defineConfig([
      globalIgnores(['dist', 'apps-script']),
      {
        files: ['**/*.{js,jsx}'],
        extends: [
          js.configs.recommended,
          reactHooks.configs.flat.recommended,
          reactRefresh.configs.vite,
          jsxA11y.flatConfigs.recommended,
        ],
        languageOptions: {
          ecmaVersion: 2020,
          globals: globals.browser,
          parserOptions: {
            ecmaVersion: 'latest',
            ecmaFeatures: { jsx: true },
            sourceType: 'module',
          },
        },
        plugins: {
          jsdoc,
        },
        rules: {
          'no-unused-vars': ['error', { varsIgnorePattern: '^[A-Z_]' }],
          'jsdoc/require-jsdoc': ['warn', {
            require: {
              FunctionDeclaration: true,
              ArrowFunctionExpression: false,
              FunctionExpression: false,
            },
            publicOnly: true,
          }],
        },
      },
    ])
    ```

    Puntos criticos:
    - `jsxA11y.flatConfigs.recommended` (NO `jsxA11y.configs.recommended` — eso es legacy ESLint 8)
    - `publicOnly: true` en require-jsdoc para eximir helpers privados como GroupSection, LoginInput, ChevronIcon
    - `ArrowFunctionExpression: false` para evitar warnings en arrow exports

    3. Ejecutar `npm run lint` para inventariar errores nuevos de jsx-a11y. Los errores esperados son:
    - `click-events-have-key-events` en TeacherCard.jsx (GroupSection divs, student divs)
    - `no-static-element-interactions` en los mismos
    - `click-events-have-key-events` en StatCard.jsx (div con onClick)
    - Posibles warnings de `jsdoc/require-jsdoc` en pages sin JSDoc

    Estos errores se resolveran en Plan 02 (ARIA mecanico en StatCard/AlertList) y Plan 03 (TeacherCard keyboard). En esta tarea, NO corregir los errores de jsx-a11y que pertenecen a otros planes.

    4. Si `npm run lint` muestra errores que bloquean (exit code != 0), evaluar si los errores son de archivos que esta tarea no toca. Si los errores vienen EXCLUSIVAMENTE de archivos modificados en Plan 02/03, eso es aceptable en este punto — los planes subsiguientes los resuelven.

    NOTA: Si hay errores inesperados de jsx-a11y que no se anticiparon en RESEARCH.md (ejemplo: `img-redundant-alt` en PageHeader logo), agregar overrides especificos en la seccion `rules` del eslint.config.js, con un comentario explicando por que.
  </action>
  <verify>
    <automated>cd C:/Users/Usuario/Desktop/novattend && node -e "const c = require('./eslint.config.js'); console.log('config loaded')" 2>/dev/null; npx vitest run 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - eslint.config.js contiene `import jsxA11y from 'eslint-plugin-jsx-a11y'`
    - eslint.config.js contiene `import jsdoc from 'eslint-plugin-jsdoc'`
    - eslint.config.js contiene `jsxA11y.flatConfigs.recommended`
    - eslint.config.js contiene `'jsdoc/require-jsdoc'`
    - eslint.config.js contiene `publicOnly: true`
    - package.json contiene `eslint-plugin-jsx-a11y` en devDependencies
    - package.json contiene `eslint-plugin-jsdoc` en devDependencies
    - npm test pasa sin regresiones
  </acceptance_criteria>
  <done>ESLint configurado con jsx-a11y recommended y jsdoc require-jsdoc. Los errores de lint de jsx-a11y en archivos que aun no se han modificado son esperados y se resuelven en Plan 02 y 03.</done>
</task>

<task type="auto">
  <name>Task 3: JSDoc en las 4 pages restantes</name>
  <files>src/pages/AttendancePage.jsx, src/pages/DashboardPage.jsx, src/pages/LoginPage.jsx, src/pages/SavedPage.jsx</files>
  <read_first>
    - src/pages/AttendancePage.jsx (linea 1-15 — confirmar que no tiene JSDoc)
    - src/pages/DashboardPage.jsx (linea 1-15 — confirmar que no tiene JSDoc)
    - src/pages/LoginPage.jsx (linea 1-15 — confirmar que no tiene JSDoc)
    - src/pages/SavedPage.jsx (linea 1-15 — confirmar que no tiene JSDoc)
    - src/pages/ConvocatoriaPage.jsx (linea 1-10 — referencia del patron JSDoc ya existente en pages)
  </read_first>
  <action>
    Agregar cabecera JSDoc ANTES de la linea `export default function` en cada page. El patron sigue el establecido en ConvocatoriaPage.jsx y los componentes ui/features.

    **AttendancePage.jsx** — insertar ANTES de `export default function AttendancePage()`:
    ```jsx
    /**
     * Pagina de marcado de asistencia para profesores.
     * Carga alumnos del grupo seleccionado, permite marcar presentes/ausentes y guardar.
     * @returns {JSX.Element}
     */
    ```

    **DashboardPage.jsx** — insertar ANTES de `export default function DashboardPage()`:
    ```jsx
    /**
     * Dashboard analitico para el CEO.
     * Muestra estadisticas globales, listado de profesores y busqueda de alumnos.
     * @returns {JSX.Element}
     */
    ```

    **LoginPage.jsx** — insertar ANTES de `export default function LoginPage()`:
    ```jsx
    /**
     * Pagina de autenticacion. Valida credenciales y redirige segun rol.
     * @returns {JSX.Element}
     */
    ```

    **SavedPage.jsx** — insertar ANTES de `export default function SavedPage()`:
    ```jsx
    /**
     * Pagina de confirmacion post-guardado de asistencia.
     * Muestra resumen de presentes/ausentes y boton de retorno.
     * @returns {JSX.Element}
     */
    ```

    Las descripciones estan en espanol (per CLAUDE.md idioma), los identificadores (@returns, JSX.Element) en ingles.
  </action>
  <verify>
    <automated>cd C:/Users/Usuario/Desktop/novattend && grep -c "@returns {JSX.Element}" src/pages/AttendancePage.jsx src/pages/DashboardPage.jsx src/pages/LoginPage.jsx src/pages/SavedPage.jsx</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/AttendancePage.jsx contiene `* Pagina de marcado de asistencia para profesores.`
    - src/pages/AttendancePage.jsx contiene `@returns {JSX.Element}`
    - src/pages/DashboardPage.jsx contiene `* Dashboard analitico para el CEO.`
    - src/pages/DashboardPage.jsx contiene `@returns {JSX.Element}`
    - src/pages/LoginPage.jsx contiene `* Pagina de autenticacion.`
    - src/pages/LoginPage.jsx contiene `@returns {JSX.Element}`
    - src/pages/SavedPage.jsx contiene `* Pagina de confirmacion post-guardado`
    - src/pages/SavedPage.jsx contiene `@returns {JSX.Element}`
    - Las warnings de jsdoc/require-jsdoc para estas 4 pages desaparecen tras el cambio
  </acceptance_criteria>
  <done>Las 4 pages restantes (Attendance, Dashboard, Login, Saved) tienen cabecera JSDoc con descripcion en espanol y @returns documentado. Junto con ConvocatoriaPage (que ya la tenia), las 5 pages cumplen DOCS-01.</done>
</task>

</tasks>

<verification>
1. `npm test` pasa sin regresiones (test suite existente verde)
2. `src/index.css` contiene `:focus-visible` con `outline: 2px solid #800000`
3. `src/components/ui/ToggleSwitch.jsx` no contiene `focus-visible:outline`
4. `eslint.config.js` importa y configura `eslint-plugin-jsx-a11y` y `eslint-plugin-jsdoc`
5. Las 4 pages tienen cabecera JSDoc con `@returns {JSX.Element}`
</verification>

<success_criteria>
- Focus ring burgundy visible al navegar con Tab en todos los elementos interactivos
- eslint-plugin-jsx-a11y y eslint-plugin-jsdoc instalados y configurados en ESLint flat config
- jsdoc/require-jsdoc configurado con publicOnly: true y FunctionDeclaration: true
- Las 4 pages sin JSDoc ahora tienen cabecera completa
- npm test pasa (0 failures)
</success_criteria>

<output>
After completion, create `.planning/phases/04-documentacion-y-accesibilidad/04-01-SUMMARY.md`
</output>
