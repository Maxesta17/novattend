---
phase: 03-arquitectura-y-accesibilidad
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useDashboard.js
  - src/pages/DashboardPage.jsx
autonomous: true
requirements: [ARCH-01]
must_haves:
  truths:
    - "DashboardPage.jsx tiene menos de 250 lineas"
    - "Toda la logica (useState, useEffect, useMemo, useCallback, loadConvData, handleConvChange) vive en useDashboard.js"
    - "DashboardPage es puro JSX orquestador — importa useDashboard, destructura, renderiza"
    - "El Dashboard muestra los mismos datos que antes del refactor — cero regresion funcional"
    - "Los 79 tests existentes pasan en verde"
  artifacts:
    - path: "src/hooks/useDashboard.js"
      provides: "Hook con toda la logica de datos/estado/handlers del Dashboard"
      exports: ["default (useDashboard)"]
      min_lines: 100
    - path: "src/pages/DashboardPage.jsx"
      provides: "Page orquestadora pura — solo JSX"
      min_lines: 50
  key_links:
    - from: "src/pages/DashboardPage.jsx"
      to: "src/hooks/useDashboard.js"
      via: "import useDashboard + destructuring del retorno"
      pattern: "import useDashboard from.*hooks/useDashboard"
    - from: "src/hooks/useDashboard.js"
      to: "src/hooks/useConvocatorias.js"
      via: "consume useConvocatorias internamente (no duplica)"
      pattern: "useConvocatorias\\(\\)"
    - from: "src/hooks/useDashboard.js"
      to: "src/hooks/useDebounce.js"
      via: "consume useDebounce para searchQuery"
      pattern: "useDebounce\\("
    - from: "src/hooks/useDashboard.js"
      to: "src/services/api.js"
      via: "llama getProfesores y getResumen"
      pattern: "getProfesores|getResumen"
---

<objective>
Extraer toda la logica de DashboardPage.jsx a un hook custom `useDashboard.js` para que la page quede como puro JSX orquestador bajo 250 lineas (cumplir CLAUDE.md y ARCH-01).

Purpose: DashboardPage tiene 247 lineas — ya roza el limite. Extraer la logica a un hook dedicado deja la page en ~100 lineas de JSX puro, mejora la testabilidad del hook por separado, y cumple el Success Criterion SC1 de la fase.

Output: `src/hooks/useDashboard.js` (~130 lineas) + `src/pages/DashboardPage.jsx` refactorizado (~100 lineas)
</objective>

<execution_context>
@C:\Users\Usuario\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Usuario\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Contratos que el executor necesita. Extraidos del codebase actual. -->

From src/hooks/useConvocatorias.js (patron de referencia para estructura del hook):
```javascript
export default function useConvocatorias() {
  // ...useState, useEffect, useCallback...
  return {
    convocatorias,
    selectedConvocatoria,
    setSelectedConvocatoria,
    loading,
    error,
    reload: load,
  }
}
```

From src/hooks/useDebounce.js:
```javascript
export default function useDebounce(value, delay) {
  // retorna valor debounced
}
```

From src/utils/buildTeachersHierarchy.js:
```javascript
export default function buildTeachersHierarchy(profesores, resumen) {
  // retorna array de teachers con groups y students
}
```

From src/services/api.js (funciones consumidas por el hook):
```javascript
export async function getProfesores() { /* ... */ }
export async function getResumen(convocatoriaId) { /* ... */ }
```

From src/config/api.js:
```javascript
export function isApiEnabled() { /* retorna boolean */ }
```

From src/config/teachers.js:
```javascript
export const TEACHERS_DATA = [ /* mock data */ ]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear hook useDashboard.js con toda la logica extraida de DashboardPage</name>
  <files>src/hooks/useDashboard.js</files>
  <read_first>
    - src/pages/DashboardPage.jsx (codigo fuente completo — la logica a extraer)
    - src/hooks/useConvocatorias.js (patron de referencia para estructura del hook)
    - src/hooks/useDebounce.js (dependencia que useDashboard importa)
    - .planning/codebase/CONVENTIONS.md (patron JSDoc, naming, imports)
  </read_first>
  <action>
Crear `src/hooks/useDashboard.js` con esta estructura exacta (per D-01, D-02):

1. **Imports** (react hooks + hooks propios + services + config + utils):
```javascript
import { useState, useEffect, useCallback, useMemo } from 'react'
import useConvocatorias from './useConvocatorias.js'
import useDebounce from './useDebounce.js'
import { TEACHERS_DATA } from '../config/teachers.js'
import { isApiEnabled } from '../config/api'
import { getProfesores, getResumen } from '../services/api'
import buildTeachersHierarchy from '../utils/buildTeachersHierarchy.js'
```

2. **JSDoc header** con `@returns` documentando el objeto completo (patron de useConvocatorias).

3. **`export default function useDashboard()`** con este contenido movido de DashboardPage:

   a. Llamar `useConvocatorias()` y destructurar renombrando `selectedConvocatoria` a `convocatoria`:
   ```javascript
   const {
     convocatorias,
     selectedConvocatoria: convocatoria,
     setSelectedConvocatoria,
     loading: convsLoading,
     error: convsError,
     reload,
   } = useConvocatorias()
   ```

   b. 7 useState (exactos de DashboardPage lineas 30-36):
   ```javascript
   const [teachers, setTeachers] = useState(null)
   const [loading, setLoading] = useState(true)
   const [error, setError] = useState(null)
   const [expandedTeacher, setExpandedTeacher] = useState(null)
   const [searchQuery, setSearchQuery] = useState('')
   const [selectedStudent, setSelectedStudent] = useState(null)
   const [showAlertPopup, setShowAlertPopup] = useState(false)
   ```

   c. `useDebounce(searchQuery, 300)` asignado a `debouncedSearch`.

   d. 5 useCallback (lineas 40-44 de DashboardPage):
   ```javascript
   const handleAlertClick = useCallback(() => setShowAlertPopup(true), [])
   const handleAlertClose = useCallback(() => setShowAlertPopup(false), [])
   const handleStudentClose = useCallback(() => setSelectedStudent(null), [])
   const handleClear = useCallback(() => setSearchQuery(''), [])
   const handleTeacherToggle = useCallback((id) => setExpandedTeacher(prev => prev === id ? null : id), [])
   ```

   e. `loadConvData` como funcion async interna (NO useCallback — solo la llaman useEffect y handleConvChange):
   ```javascript
   const loadConvData = async (conv) => {
     const [profesores, resumen] = await Promise.all([
       getProfesores(),
       getResumen(conv.id),
     ])
     setTeachers(buildTeachersHierarchy(profesores || [], resumen || []))
   }
   ```

   f. useEffect reactivo (lineas 56-89 de DashboardPage). Mantener el `// eslint-disable-line react-hooks/exhaustive-deps` en la linea de cierre del array de dependencias con comentario explicativo:
   ```javascript
   // eslint-disable-line react-hooks/exhaustive-deps — loadConvData es local sin useCallback, deps reales son convsLoading/convsError/convocatoria
   ```

   g. `handleConvChange` como funcion async (lineas 92-105 de DashboardPage):
   ```javascript
   const handleConvChange = async (conv) => {
     setSelectedConvocatoria(conv)
     setLoading(true)
     setError(null)
     setExpandedTeacher(null)
     setSearchQuery('')
     try {
       await loadConvData(conv)
     } catch (err) {
       setError(err.message || 'Error al cargar datos')
     } finally {
       setLoading(false)
     }
   }
   ```

   h. 5 useMemo (lineas 107-143 de DashboardPage): `totalStudents`, `globalAttendance`, `allStudents`, `alertStudents`, `searchResults` — copiar exactamente la logica.

   i. **Return object** con todos los valores que DashboardPage necesita:
   ```javascript
   return {
     convocatorias,
     convocatoria,
     reload,
     teachers,
     loading,
     error,
     expandedTeacher,
     searchQuery,
     setSearchQuery,
     selectedStudent,
     setSelectedStudent,
     showAlertPopup,
     handleAlertClick,
     handleAlertClose,
     handleStudentClose,
     handleClear,
     handleTeacherToggle,
     handleConvChange,
     totalStudents,
     globalAttendance,
     alertStudents,
     searchResults,
   }
   ```

**IMPORTANTE:** No duplicar la logica de useConvocatorias. useDashboard llama `useConvocatorias()` internamente y re-expone `convocatorias`, `convocatoria`, `reload`.

**IMPORTANTE:** `useNavigate` NO va en el hook (per Pitfall 2 del RESEARCH). La navegacion queda en DashboardPage.
  </action>
  <verify>
    <automated>cd "C:\Users\Usuario\Desktop\novattend" && node -e "const m = await import('./src/hooks/useDashboard.js'); console.log(typeof m.default)" 2>&1 || echo "Module check failed" && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/hooks/useDashboard.js` existe y tiene entre 100-160 lineas
    - El archivo contiene `export default function useDashboard()`
    - El archivo contiene `useConvocatorias()` (consume, no duplica)
    - El archivo contiene `useDebounce(searchQuery, 300)`
    - El archivo contiene los 5 useCallback: `handleAlertClick`, `handleAlertClose`, `handleStudentClose`, `handleClear`, `handleTeacherToggle`
    - El archivo contiene `loadConvData` como funcion async (no useCallback)
    - El archivo contiene un useEffect con `[convsLoading, convsError, convocatoria]`
    - El archivo contiene `handleConvChange` como funcion async
    - El archivo contiene 5 useMemo: `totalStudents`, `globalAttendance`, `allStudents`, `alertStudents`, `searchResults`
    - El archivo contiene JSDoc con `@returns` documentando el shape del objeto
    - El return object incluye los 22 keys listados en el action
    - El archivo NO contiene `useNavigate`
    - `npm run lint` pasa sin errores
  </acceptance_criteria>
  <done>useDashboard.js existe con toda la logica extraida de DashboardPage, linter pasa, y el hook exporta la funcion por defecto</done>
</task>

<task type="auto">
  <name>Task 2: Refactorizar DashboardPage para consumir useDashboard — puro JSX orquestador</name>
  <files>src/pages/DashboardPage.jsx</files>
  <read_first>
    - src/hooks/useDashboard.js (el hook recien creado — verificar el return object)
    - src/pages/DashboardPage.jsx (estado actual para no perder JSX)
    - .planning/codebase/CONVENTIONS.md (patron de imports)
  </read_first>
  <action>
Reescribir `src/pages/DashboardPage.jsx` como puro JSX orquestador (per D-02, D-03):

1. **Imports** — reemplazar todos los imports de hooks/services/config por un solo import del hook:
```javascript
import { useNavigate } from 'react-router-dom'
import useDashboard from '../hooks/useDashboard.js'
import PageHeader from '../components/features/PageHeader.jsx'
import TeacherCard from '../components/features/TeacherCard.jsx'
import StudentDetailPopup from '../components/features/StudentDetailPopup.jsx'
import AlertList from '../components/features/AlertList.jsx'
import StatCard from '../components/ui/StatCard.jsx'
import Badge from '../components/ui/Badge.jsx'
import SearchInput from '../components/ui/SearchInput.jsx'
import ConvocatoriaSelector from '../components/features/ConvocatoriaSelector.jsx'
import DashboardSkeleton from '../components/features/DashboardSkeleton.jsx'
```

Eliminar estos imports que ya no se necesitan en la page (ahora estan en useDashboard):
- `useState, useMemo, useEffect, useCallback` de react
- `useDebounce` de hooks
- `TEACHERS_DATA` de config/teachers
- `isApiEnabled` de config/api
- `getProfesores, getResumen` de services/api
- `useConvocatorias` de hooks
- `buildTeachersHierarchy` de utils

2. **Cuerpo de la funcion** — destructurar useDashboard y mantener solo useNavigate:
```javascript
export default function DashboardPage() {
  const navigate = useNavigate()
  const {
    convocatorias, convocatoria, reload,
    teachers, loading, error,
    expandedTeacher, searchQuery, setSearchQuery,
    selectedStudent, setSelectedStudent, showAlertPopup,
    handleAlertClick, handleAlertClose, handleStudentClose,
    handleClear, handleTeacherToggle, handleConvChange,
    totalStudents, globalAttendance, alertStudents, searchResults,
  } = useDashboard()
```

3. **JSX** — mantener EXACTAMENTE el mismo JSX que existe actualmente (lineas 145-246 de DashboardPage). No cambiar nada del renderizado — ni clases, ni estructura, ni handlers. El handler de logout sigue inline: `() => { sessionStorage.removeItem('user'); navigate('/') }`.

4. **Verificar** que el archivo resultante tiene ~100 lineas (imports + destructuring + loading/error guards + JSX return).

**CRITICO:** No extraer subcomponentes del JSX (per D-03). Solo el hook. El JSX queda exacto.
  </action>
  <verify>
    <automated>cd "C:\Users\Usuario\Desktop\novattend" && npm run lint 2>&1 | tail -5 && npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/DashboardPage.jsx` tiene menos de 150 lineas (target ~100)
    - El archivo contiene `import useDashboard from '../hooks/useDashboard.js'`
    - El archivo contiene `import { useNavigate } from 'react-router-dom'`
    - El archivo NO contiene `useState`, `useMemo`, `useEffect`, `useCallback` en imports
    - El archivo NO contiene `import useConvocatorias`
    - El archivo NO contiene `import useDebounce`
    - El archivo NO contiene `import { getProfesores` ni `import { getResumen`
    - El archivo NO contiene `import { isApiEnabled`
    - El archivo NO contiene `import { TEACHERS_DATA`
    - El archivo NO contiene `import buildTeachersHierarchy`
    - El archivo contiene `const navigate = useNavigate()` (unico hook de React Router en la page)
    - El archivo contiene `const { convocatorias, convocatoria, reload` (destructuring de useDashboard)
    - El archivo contiene `sessionStorage.removeItem('user')` en el handler de logout
    - `npm run lint` pasa sin errores
    - `npm test` — 79 tests, 13 suites, todos verdes
  </acceptance_criteria>
  <done>DashboardPage tiene menos de 150 lineas, es puro JSX orquestador sin logica de negocio, los 79 tests pasan en verde, y lint pasa sin errores</done>
</task>

</tasks>

<verification>
1. `wc -l src/pages/DashboardPage.jsx` — debe ser menor a 150 (target ~100)
2. `wc -l src/hooks/useDashboard.js` — debe ser entre 100-160
3. `npm run lint` — cero errores
4. `npm test` — 79 tests, 13 suites, todos verdes
5. `npm run build` — build exitoso sin errores
6. Verificar que DashboardPage no contiene useState/useEffect/useMemo/useCallback
7. Verificar que useDashboard no contiene useNavigate
</verification>

<success_criteria>
- DashboardPage.jsx tiene menos de 250 lineas (SC1 de la fase) — target ~100
- Toda la logica vive en useDashboard.js (SC1 — "logica vive en useDashboard.js")
- Los 79 tests existentes pasan en verde (SC3 de la fase)
- Lint pasa sin errores nuevos
- Build de produccion exitoso
</success_criteria>

<output>
After completion, create `.planning/phases/03-arquitectura-y-accesibilidad/03-01-SUMMARY.md`
</output>
