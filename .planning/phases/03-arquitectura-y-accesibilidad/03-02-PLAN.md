---
phase: 03-arquitectura-y-accesibilidad
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useFocusTrap.js
  - src/components/ui/Modal.jsx
  - src/components/features/AlertList.jsx
  - src/components/features/StudentDetailPopup.jsx
autonomous: true
requirements: [ARCH-02]
must_haves:
  truths:
    - "Al abrir un modal, el foco se mueve al primer elemento focusable dentro del modal"
    - "Tab y Shift+Tab ciclan entre los elementos focusables del modal sin escapar al fondo"
    - "Presionar Escape cierra el modal"
    - "El Modal tiene role=dialog y aria-modal=true para screen readers"
    - "AlertList y StudentDetailPopup pasan ariaLabel descriptivo al Modal"
    - "Los 79 tests existentes pasan en verde"
  artifacts:
    - path: "src/hooks/useFocusTrap.js"
      provides: "Hook custom que atrapa foco dentro de un contenedor"
      exports: ["default (useFocusTrap)"]
      min_lines: 40
    - path: "src/components/ui/Modal.jsx"
      provides: "Modal accesible con focus trap, ARIA, y Escape"
      exports: ["default (Modal)"]
      contains: "role=\"dialog\""
    - path: "src/components/features/AlertList.jsx"
      provides: "Consumidor de Modal con ariaLabel"
      contains: "ariaLabel="
    - path: "src/components/features/StudentDetailPopup.jsx"
      provides: "Consumidor de Modal con ariaLabel"
      contains: "ariaLabel="
  key_links:
    - from: "src/components/ui/Modal.jsx"
      to: "src/hooks/useFocusTrap.js"
      via: "import + llamada al hook con isOpen y onClose"
      pattern: "useFocusTrap\\(isOpen.*onClose\\)"
    - from: "src/components/features/AlertList.jsx"
      to: "src/components/ui/Modal.jsx"
      via: "prop ariaLabel en <Modal>"
      pattern: "ariaLabel="
    - from: "src/components/features/StudentDetailPopup.jsx"
      to: "src/components/ui/Modal.jsx"
      via: "prop ariaLabel en <Modal>"
      pattern: "ariaLabel="
---

<objective>
Hacer el componente Modal accesible por teclado implementando un focus trap custom (Tab/Shift+Tab atrapados, Escape cierra) y agregando atributos ARIA minimos (role="dialog", aria-modal="true", aria-label).

Purpose: El Modal actual no tiene soporte de teclado — un usuario que navega con Tab escapa al fondo de la pagina, y no hay forma de cerrar con Escape. Esto cumple ARCH-02 y SC2 de la fase.

Output: `src/hooks/useFocusTrap.js` (hook custom ~55 lineas), `src/components/ui/Modal.jsx` actualizado (~45 lineas), AlertList y StudentDetailPopup con ariaLabel.
</objective>

<execution_context>
@C:\Users\Usuario\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Usuario\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Contratos actuales del Modal y sus consumidores. -->

From src/components/ui/Modal.jsx (API actual que debe mantenerse):
```javascript
export default function Modal({
  isOpen,
  onClose,
  children,
  maxWidth = '360px',
  className = '',
})
```

From src/components/features/AlertList.jsx (uso actual del Modal):
```javascript
<Modal isOpen onClose={onClose} className="max-h-[70vh] overflow-auto">
```

From src/components/features/StudentDetailPopup.jsx (uso actual del Modal):
```javascript
<Modal isOpen onClose={onClose}>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear hook useFocusTrap.js — atrapa foco + Escape + restauracion</name>
  <files>src/hooks/useFocusTrap.js</files>
  <read_first>
    - src/components/ui/Modal.jsx (el componente que consumira el hook — ver API)
    - src/hooks/useConvocatorias.js (patron de referencia para estructura de hooks custom)
    - .planning/codebase/CONVENTIONS.md (JSDoc, naming, import style)
    - .planning/phases/03-arquitectura-y-accesibilidad/03-RESEARCH.md (seccion "Patron del hook useFocusTrap" — implementacion de referencia completa)
  </read_first>
  <action>
Crear `src/hooks/useFocusTrap.js` con implementacion custom sin dependencias externas (per D-04):

```javascript
import { useEffect, useRef } from 'react'

/**
 * Hook que atrapa el foco dentro de un contenedor mientras el modal esta abierto.
 * Maneja Tab/Shift+Tab ciclico, Escape para cerrar, y restaura el foco al cerrar.
 * @param {boolean} isOpen - Activa/desactiva el trap
 * @param {function} onClose - Callback al presionar Escape
 * @returns {React.RefObject} ref para asignar al contenedor del modal
 */
export default function useFocusTrap(isOpen, onClose) {
  const containerRef = useRef(null)

  useEffect(() => {
    if (!isOpen || !containerRef.current) return

    // Guardar el elemento que tenia el foco antes de abrir (para restaurar al cerrar)
    const previouslyFocused = document.activeElement

    // Selectores de elementos focusables segun ARIA Authoring Practices
    const FOCUSABLE = [
      'a[href]',
      'button:not([disabled])',
      'input:not([disabled])',
      'select:not([disabled])',
      'textarea:not([disabled])',
      '[tabindex]:not([tabindex="-1"])',
    ].join(', ')

    const container = containerRef.current

    // Foco inicial al primer elemento focusable (per D-05)
    const focusables = Array.from(container.querySelectorAll(FOCUSABLE))
    if (focusables.length > 0) {
      focusables[0].focus()
    } else {
      // Si no hay elementos focusables, foco al contenedor (necesita tabIndex={-1})
      container.focus()
    }

    const handleKeyDown = (e) => {
      if (e.key === 'Escape') {
        e.preventDefault()
        onClose()
        return
      }

      if (e.key !== 'Tab') return

      // Re-query en cada keydown para capturar contenido dinamico (per Pitfall 3 del RESEARCH)
      const focusableNow = Array.from(container.querySelectorAll(FOCUSABLE))
      if (focusableNow.length === 0) return

      const first = focusableNow[0]
      const last = focusableNow[focusableNow.length - 1]

      if (e.shiftKey) {
        if (document.activeElement === first) {
          e.preventDefault()
          last.focus()
        }
      } else {
        if (document.activeElement === last) {
          e.preventDefault()
          first.focus()
        }
      }
    }

    document.addEventListener('keydown', handleKeyDown)

    return () => {
      document.removeEventListener('keydown', handleKeyDown)
      // Restaurar foco al elemento que lo tenia antes de abrir el modal
      if (previouslyFocused && typeof previouslyFocused.focus === 'function') {
        previouslyFocused.focus()
      }
    }
  }, [isOpen, onClose])

  return containerRef
}
```

**Puntos criticos:**
- `FOCUSABLE` usa los 6 selectores estandar WAI-ARIA, no inventar otros.
- `focusableNow` se recalcula en CADA keydown (no cachear) — contenido del modal puede cambiar (StudentDetailPopup carga faltas async).
- `previouslyFocused` se captura al ABRIR y se restaura al CERRAR en el cleanup del useEffect.
- El `tabIndex={-1}` en el contenedor lo agrega Modal.jsx, no el hook. El hook solo asigna el ref.
- El hook NO importa nada del proyecto (solo React). Es 100% reutilizable.
  </action>
  <verify>
    <automated>cd "C:\Users\Usuario\Desktop\novattend" && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/hooks/useFocusTrap.js` existe y tiene entre 40-70 lineas
    - El archivo contiene `export default function useFocusTrap(isOpen, onClose)`
    - El archivo contiene `import { useEffect, useRef } from 'react'`
    - El archivo contiene `const containerRef = useRef(null)`
    - El archivo contiene `const FOCUSABLE =` con los 6 selectores: `a[href]`, `button:not([disabled])`, `input:not([disabled])`, `select:not([disabled])`, `textarea:not([disabled])`, `[tabindex]:not([tabindex="-1"])`
    - El archivo contiene `e.key === 'Escape'` seguido de `onClose()`
    - El archivo contiene `e.key !== 'Tab'`
    - El archivo contiene `container.querySelectorAll(FOCUSABLE)` DENTRO del handleKeyDown (re-query dinamico)
    - El archivo contiene `const previouslyFocused = document.activeElement` (captura foco previo)
    - El archivo contiene `previouslyFocused.focus()` en el cleanup (restauracion)
    - El archivo contiene JSDoc con `@param {boolean} isOpen` y `@param {function} onClose` y `@returns`
    - El archivo retorna `containerRef`
    - `npm run lint` pasa sin errores
  </acceptance_criteria>
  <done>useFocusTrap.js existe con focus trap completo (Tab ciclico, Escape cierra, foco inicial, restauracion), linter pasa, sin dependencias externas</done>
</task>

<task type="auto">
  <name>Task 2: Actualizar Modal con useFocusTrap + ARIA, y consumidores con ariaLabel</name>
  <files>src/components/ui/Modal.jsx, src/components/features/AlertList.jsx, src/components/features/StudentDetailPopup.jsx</files>
  <read_first>
    - src/hooks/useFocusTrap.js (hook recien creado — verificar la API)
    - src/components/ui/Modal.jsx (estado actual — mantener API compatible)
    - src/components/features/AlertList.jsx (consumidor — agregar ariaLabel)
    - src/components/features/StudentDetailPopup.jsx (consumidor — agregar ariaLabel)
  </read_first>
  <action>
**Archivo 1: src/components/ui/Modal.jsx** — Integrar useFocusTrap + ARIA (per D-06, D-07):

Reescribir Modal.jsx con estos cambios:

```javascript
import useFocusTrap from '../../hooks/useFocusTrap.js'

/**
 * Modal/popup con overlay oscuro, focus trap y cierre con Escape.
 * @param {object} props
 * @param {boolean} props.isOpen - Controla visibilidad
 * @param {function} props.onClose - Handler al cerrar (click en overlay o Escape)
 * @param {React.ReactNode} props.children - Contenido del modal
 * @param {string} [props.maxWidth='360px'] - Ancho maximo del contenedor
 * @param {string} [props.className] - Clases adicionales para el contenedor interno
 * @param {string} [props.ariaLabel=''] - Label accesible para screen readers
 */
export default function Modal({
  isOpen,
  onClose,
  children,
  maxWidth = '360px',
  className = '',
  ariaLabel = '',
}) {
  const containerRef = useFocusTrap(isOpen, onClose)

  if (!isOpen) return null

  return (
    <div
      className="animate-fade-in fixed inset-0 bg-black/60 flex items-center justify-center z-40 p-5"
      onClick={onClose}
    >
      <div
        ref={containerRef}
        role="dialog"
        aria-modal="true"
        aria-label={ariaLabel}
        tabIndex={-1}
        className={`animate-pop-up bg-white rounded-[20px] p-6 w-full shadow-2xl ${className}`}
        style={{ maxWidth }}
        onClick={e => e.stopPropagation()}
      >
        {children}
      </div>
    </div>
  )
}
```

Cambios respecto al actual:
- Agregar `import useFocusTrap from '../../hooks/useFocusTrap.js'`
- Agregar prop `ariaLabel = ''` con default vacio (no rompe consumidores existentes)
- Agregar `const containerRef = useFocusTrap(isOpen, onClose)` despues del destructuring
- Agregar `ref={containerRef}` al div interno
- Agregar `role="dialog"` al div interno
- Agregar `aria-modal="true"` al div interno
- Agregar `aria-label={ariaLabel}` al div interno
- Agregar `tabIndex={-1}` al div interno (per Pitfall 4 — necesario si no hay focusables)
- Actualizar JSDoc para documentar `ariaLabel` y mencionar focus trap/Escape
- NO tocar el `style={{ maxWidth }}` — excepcion documentada en CLAUDE.md
- NO agregar tabIndex al overlay exterior — solo al contenedor interno

**Archivo 2: src/components/features/AlertList.jsx** — Agregar ariaLabel (per D-08):

Cambiar la linea del `<Modal>` de:
```javascript
<Modal isOpen onClose={onClose} className="max-h-[70vh] overflow-auto">
```
a:
```javascript
<Modal isOpen onClose={onClose} className="max-h-[70vh] overflow-auto" ariaLabel="Lista de alumnos en alerta">
```

Un solo cambio en una linea. No tocar nada mas del componente.

**Archivo 3: src/components/features/StudentDetailPopup.jsx** — Agregar ariaLabel (per D-08):

Cambiar la linea del `<Modal>` de:
```javascript
<Modal isOpen onClose={onClose}>
```
a:
```javascript
<Modal isOpen onClose={onClose} ariaLabel="Detalle de asistencia del alumno">
```

Un solo cambio en una linea. No tocar nada mas del componente.
  </action>
  <verify>
    <automated>cd "C:\Users\Usuario\Desktop\novattend" && npm run lint 2>&1 | tail -5 && npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ui/Modal.jsx` contiene `import useFocusTrap from '../../hooks/useFocusTrap.js'`
    - `src/components/ui/Modal.jsx` contiene `ariaLabel = ''` en los params destructurados
    - `src/components/ui/Modal.jsx` contiene `const containerRef = useFocusTrap(isOpen, onClose)`
    - `src/components/ui/Modal.jsx` contiene `ref={containerRef}` en el div interno
    - `src/components/ui/Modal.jsx` contiene `role="dialog"` en el div interno
    - `src/components/ui/Modal.jsx` contiene `aria-modal="true"` en el div interno
    - `src/components/ui/Modal.jsx` contiene `aria-label={ariaLabel}` en el div interno
    - `src/components/ui/Modal.jsx` contiene `tabIndex={-1}` en el div interno (NO en el overlay)
    - `src/components/ui/Modal.jsx` tiene menos de 50 lineas (bajo limite 250 CLAUDE.md)
    - `src/components/features/AlertList.jsx` contiene `ariaLabel="Lista de alumnos en alerta"`
    - `src/components/features/StudentDetailPopup.jsx` contiene `ariaLabel="Detalle de asistencia del alumno"`
    - `npm run lint` pasa sin errores
    - `npm test` — 79 tests, 13 suites, todos verdes
  </acceptance_criteria>
  <done>Modal tiene focus trap operativo (Tab ciclico, Escape cierra), ARIA role/aria-modal/aria-label, y los dos consumidores pasan ariaLabel descriptivo. Los 79 tests existentes pasan en verde.</done>
</task>

</tasks>

<verification>
1. `npm run lint` — cero errores en archivos modificados
2. `npm test` — 79 tests, 13 suites, todos verdes
3. `npm run build` — build exitoso sin errores
4. Verificar que Modal.jsx tiene `role="dialog"`, `aria-modal="true"`, `aria-label`, `tabIndex={-1}`, `ref={containerRef}`
5. Verificar que useFocusTrap.js tiene manejo de Tab, Shift+Tab, Escape, foco inicial, y restauracion
6. Verificar que AlertList tiene `ariaLabel="Lista de alumnos en alerta"`
7. Verificar que StudentDetailPopup tiene `ariaLabel="Detalle de asistencia del alumno"`
</verification>

<success_criteria>
- Al abrir un modal, el foco queda atrapado dentro — Tab no escapa al fondo y Escape cierra el modal (SC2 de la fase)
- Modal renderiza role="dialog" y aria-modal="true" para screen readers
- AlertList y StudentDetailPopup pasan label descriptivo al Modal
- Los 79 tests existentes pasan en verde (SC3 de la fase)
- Lint y build exitosos
</success_criteria>

<output>
After completion, create `.planning/phases/03-arquitectura-y-accesibilidad/03-02-SUMMARY.md`
</output>
