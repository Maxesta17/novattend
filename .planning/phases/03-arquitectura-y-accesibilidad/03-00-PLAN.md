---
phase: 03-arquitectura-y-accesibilidad
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/tests/useDashboard.test.jsx
  - src/tests/useFocusTrap.test.jsx
  - src/tests/Modal.test.jsx
autonomous: true
requirements: [ARCH-01, ARCH-02]
must_haves:
  truths:
    - "Existen test stubs que validan la interfaz publica de useDashboard, useFocusTrap, y Modal"
    - "Los tests fallan (RED) porque los modulos aun no existen o no cumplen los contratos"
    - "npm test ejecuta los 3 archivos nuevos sin crash de sintaxis"
  artifacts:
    - path: "src/tests/useDashboard.test.jsx"
      provides: "Tests stub para la interfaz del hook useDashboard"
      min_lines: 15
    - path: "src/tests/useFocusTrap.test.jsx"
      provides: "Tests stub para Tab trap, Escape, y restauracion de foco"
      min_lines: 15
    - path: "src/tests/Modal.test.jsx"
      provides: "Tests stub para role=dialog, aria-modal, y ariaLabel prop"
      min_lines: 15
  key_links:
    - from: "src/tests/useDashboard.test.jsx"
      to: "src/hooks/useDashboard.js"
      via: "import del hook (fallara hasta que 03-01 lo cree)"
      pattern: "import useDashboard"
    - from: "src/tests/useFocusTrap.test.jsx"
      to: "src/hooks/useFocusTrap.js"
      via: "import del hook (fallara hasta que 03-02 lo cree)"
      pattern: "import useFocusTrap"
    - from: "src/tests/Modal.test.jsx"
      to: "src/components/ui/Modal.jsx"
      via: "import del componente existente (tests validaran ARIA tras 03-02)"
      pattern: "import Modal"
---

<objective>
Crear test stubs (RED) para los tres modulos nuevos/modificados de la Fase 3: useDashboard, useFocusTrap, y Modal con ARIA. Los tests definen los contratos que los planes 03-01 y 03-02 deben cumplir.

Purpose: Cumplir Nyquist — cada tarea de implementacion necesita un `npm test` automatizado que valide su output. Sin estos stubs, las tareas de 03-01 y 03-02 no tienen test coverage automatizado.

Output: 3 archivos de test en `src/tests/` que fallan (RED) y pasan a verde (GREEN) cuando 03-01 y 03-02 completen su trabajo.
</objective>

<execution_context>
@C:\Users\Usuario\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Usuario\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/03-arquitectura-y-accesibilidad/03-VALIDATION.md

<interfaces>
<!-- Contratos que los tests deben validar. Extraidos de los planes 03-01 y 03-02. -->

useDashboard.js (plan 03-01) debe retornar este objeto:
```javascript
{
  convocatorias, convocatoria, reload,
  teachers, loading, error,
  expandedTeacher, searchQuery, setSearchQuery,
  selectedStudent, setSelectedStudent, showAlertPopup,
  handleAlertClick, handleAlertClose, handleStudentClose,
  handleClear, handleTeacherToggle, handleConvChange,
  totalStudents, globalAttendance, alertStudents, searchResults,
}
```

useFocusTrap.js (plan 03-02) debe:
- Aceptar `(isOpen, onClose)`
- Retornar un `ref` object
- Atrapar Tab/Shift+Tab entre focusables
- Llamar `onClose` al presionar Escape

Modal.jsx (plan 03-02) debe tener:
- `role="dialog"` en el contenedor interno
- `aria-modal="true"` en el contenedor interno
- Prop `ariaLabel` que se mapea a `aria-label`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear 3 archivos de test stub — useDashboard, useFocusTrap, Modal ARIA</name>
  <files>src/tests/useDashboard.test.jsx, src/tests/useFocusTrap.test.jsx, src/tests/Modal.test.jsx</files>
  <read_first>
    - src/tests/StatCard.test.jsx (patron de referencia para estructura de tests)
    - src/tests/setup.js (confirmar setup global)
    - src/components/ui/Modal.jsx (componente existente — para test de ARIA que validara estado actual vs post-03-02)
    - src/hooks/useConvocatorias.js (patron de hooks — para entender dependencias del renderHook)
  </read_first>
  <action>
Crear 3 archivos de test siguiendo el patron del proyecto (Vitest + Testing Library). Los tests definen contratos — deben ser ejecutables sin crash de sintaxis pero PUEDEN fallar (RED) si el modulo no existe aun.

**NOTA IMPORTANTE sobre estrategia RED:** Los tests de useDashboard y useFocusTrap importan modulos que AUN NO EXISTEN. Para que `npm test` no crashee en Wave 0, cada test que importa un modulo inexistente debe usar un try/catch o `vi.mock` con fallback. La estrategia recomendada es:
- Para useDashboard y useFocusTrap: usar `describe.skip` con comentario `// RED — habilitado cuando 03-01/03-02 cree el modulo`, asi npm test pasa sin crash pero el test esta marcado como pendiente.
- Para Modal: el componente YA EXISTE, asi que los tests de ARIA deben ejecutarse y FALLAR (red real) — validando que role="dialog" y aria-modal no estan presentes aun.

**Archivo 1: src/tests/useDashboard.test.jsx**

```javascript
import { describe, it, expect } from 'vitest'

// RED — descomentar cuando 03-01 cree src/hooks/useDashboard.js
// import { renderHook } from '@testing-library/react'
// import useDashboard from '../hooks/useDashboard.js'

describe.skip('useDashboard', () => {
  it('retorna el objeto con todas las keys esperadas', () => {
    // const { result } = renderHook(() => useDashboard())
    const expectedKeys = [
      'convocatorias', 'convocatoria', 'reload',
      'teachers', 'loading', 'error',
      'expandedTeacher', 'searchQuery', 'setSearchQuery',
      'selectedStudent', 'setSelectedStudent', 'showAlertPopup',
      'handleAlertClick', 'handleAlertClose', 'handleStudentClose',
      'handleClear', 'handleTeacherToggle', 'handleConvChange',
      'totalStudents', 'globalAttendance', 'alertStudents', 'searchResults',
    ]
    expectedKeys.forEach(key => {
      expect(result.current).toHaveProperty(key)
    })
  })

  it('loading inicia como true', () => {
    // const { result } = renderHook(() => useDashboard())
    expect(result.current.loading).toBe(true)
  })

  it('searchQuery inicia como string vacio', () => {
    // const { result } = renderHook(() => useDashboard())
    expect(result.current.searchQuery).toBe('')
  })
})
```

**Archivo 2: src/tests/useFocusTrap.test.jsx**

```javascript
import { describe, it, expect, vi } from 'vitest'

// RED — descomentar cuando 03-02 cree src/hooks/useFocusTrap.js
// import { renderHook } from '@testing-library/react'
// import useFocusTrap from '../hooks/useFocusTrap.js'

describe.skip('useFocusTrap', () => {
  it('retorna un ref object', () => {
    const onClose = vi.fn()
    // const { result } = renderHook(() => useFocusTrap(true, onClose))
    expect(result.current).toHaveProperty('current')
  })

  it('llama onClose al presionar Escape', () => {
    const onClose = vi.fn()
    // const { result } = renderHook(() => useFocusTrap(true, onClose))
    document.dispatchEvent(new KeyboardEvent('keydown', { key: 'Escape' }))
    expect(onClose).toHaveBeenCalledOnce()
  })
})
```

**Archivo 3: src/tests/Modal.test.jsx**

Este archivo SI ejecuta tests reales porque Modal.jsx ya existe. Los tests de ARIA fallaran (RED) hasta que 03-02 agregue los atributos:

```javascript
import { describe, it, expect } from 'vitest'
import { render, screen } from '@testing-library/react'
import Modal from '../components/ui/Modal.jsx'

describe('Modal', () => {
  it('renderiza children cuando isOpen es true', () => {
    render(
      <Modal isOpen onClose={() => {}}>
        <p>Contenido del modal</p>
      </Modal>
    )
    expect(screen.getByText('Contenido del modal')).toBeInTheDocument()
  })

  it('no renderiza nada cuando isOpen es false', () => {
    render(
      <Modal isOpen={false} onClose={() => {}}>
        <p>Oculto</p>
      </Modal>
    )
    expect(screen.queryByText('Oculto')).not.toBeInTheDocument()
  })

  it('tiene role="dialog" en el contenedor interno', () => {
    render(
      <Modal isOpen onClose={() => {}} ariaLabel="Test modal">
        <p>Contenido</p>
      </Modal>
    )
    expect(screen.getByRole('dialog')).toBeInTheDocument()
  })

  it('tiene aria-modal="true"', () => {
    render(
      <Modal isOpen onClose={() => {}} ariaLabel="Test modal">
        <p>Contenido</p>
      </Modal>
    )
    const dialog = screen.getByRole('dialog')
    expect(dialog).toHaveAttribute('aria-modal', 'true')
  })

  it('aplica aria-label desde prop ariaLabel', () => {
    render(
      <Modal isOpen onClose={() => {}} ariaLabel="Lista de alertas">
        <p>Contenido</p>
      </Modal>
    )
    const dialog = screen.getByRole('dialog')
    expect(dialog).toHaveAttribute('aria-label', 'Lista de alertas')
  })
})
```

**Criterio de exito Wave 0:** `npm test` ejecuta sin crash. Los tests de Modal ARIA fallan (RED) porque aun no tienen role="dialog". Los tests de useDashboard y useFocusTrap estan skipped. Esto es correcto — pasan a GREEN cuando 03-01 y 03-02 completen.
  </action>
  <verify>
    <automated>cd "C:\Users\Usuario\Desktop\novattend" && npm test 2>&1 | tail -20</automated>
  </verify>
  <done>Los 3 archivos de test existen. `npm test` ejecuta sin crash de sintaxis. Los tests de Modal ARIA estan en RED (fallan por falta de role="dialog"). Los tests de useDashboard y useFocusTrap estan skipped. Los 79 tests existentes siguen verdes.</done>
</task>

</tasks>

<verification>
1. `ls src/tests/useDashboard.test.jsx src/tests/useFocusTrap.test.jsx src/tests/Modal.test.jsx` — los 3 existen
2. `npm test` — ejecuta sin crash; tests de Modal ARIA en RED, hooks en skip, suite existente verde
3. `npm run lint` — cero errores en archivos nuevos
</verification>

<success_criteria>
- Los 3 archivos de test existen en src/tests/
- npm test ejecuta sin crash (los skip no cuentan como fallo)
- Los tests de Modal definen los contratos ARIA que 03-02 debe cumplir
- Los tests de hooks definen los contratos de interfaz que 03-01 y 03-02 deben cumplir
- Los 79 tests existentes siguen pasando
</success_criteria>

<output>
After completion, create `.planning/phases/03-arquitectura-y-accesibilidad/03-00-SUMMARY.md`
</output>
