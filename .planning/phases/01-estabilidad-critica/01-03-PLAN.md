---
phase: 01-estabilidad-critica
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/NotFoundPage.jsx
  - src/App.jsx
  - vite.config.js
  - src/tests/NotFoundPage.test.jsx
autonomous: true
requirements:
  - ERR-04
  - PWA-01
  - PWA-02
  - PWA-03

must_haves:
  truths:
    - "Una URL invalida como /foo muestra pagina 404 con heading '404', mensaje 'Pagina no encontrada' y boton que lleva a /"
    - "La ruta catch-all no esta protegida — cualquier usuario (con o sin sesion) ve el 404"
    - "navigateFallback es /index.html — la SPA funciona offline sin mostrar offline.html"
    - "El regex de runtime caching captura script.googleusercontent.com ademas de script.google.com"
    - "El manifest PWA tiene start_url, scope y lang='es'"
  artifacts:
    - path: "src/pages/NotFoundPage.jsx"
      provides: "Pagina 404 branded minima"
      contains: "404"
      min_lines: 15
    - path: "src/App.jsx"
      provides: "Ruta catch-all para 404"
      contains: 'path="*"'
    - path: "vite.config.js"
      provides: "Configuracion PWA corregida"
      contains: "navigateFallback: '/index.html'"
  key_links:
    - from: "src/App.jsx"
      to: "src/pages/NotFoundPage.jsx"
      via: "Route path='*' monta NotFoundPage"
      pattern: "NotFoundPage"
    - from: "vite.config.js"
      to: "service worker generado"
      via: "VitePWA config genera SW con navigateFallback y runtimeCaching"
      pattern: "navigateFallback.*index\\.html"
---

<objective>
Pagina 404 amigable + configuracion PWA que habilite offline real y cache de API.

Purpose: Actualmente URLs invalidas muestran una pagina en blanco (sin ruta catch-all). La PWA sirve offline.html como fallback en lugar del app shell (index.html), y el regex de cache no captura las redirecciones de Google Apps Script a googleusercontent.com. El manifest carece de start_url, scope y lang.
Output: NotFoundPage.jsx branded, ruta catch-all en App.jsx, vite.config.js con 3 fixes PWA.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/App.jsx
@vite.config.js
@src/components/ui/Button.jsx

<interfaces>
<!-- Estructura actual de App.jsx que el executor debe modificar -->

De src/App.jsx — imports y Routes actuales:
```jsx
import { BrowserRouter, Routes, Route } from 'react-router-dom'
import MobileContainer from './components/MobileContainer'
import ProtectedRoute from './components/ProtectedRoute'
import LoginPage from './pages/LoginPage'
// ... 4 paginas mas ...

// Dentro de <Routes>:
<Route path="/" element={<LoginPage />} />
<Route path="/convocatorias" element={<ProtectedRoute ...><ConvocatoriaPage /></ProtectedRoute>} />
<Route path="/attendance" element={<ProtectedRoute ...><AttendancePage /></ProtectedRoute>} />
<Route path="/saved" element={<ProtectedRoute ...><SavedPage /></ProtectedRoute>} />
<Route path="/dashboard" element={<ProtectedRoute ...><DashboardPage /></ProtectedRoute>} />
// AGREGAR: <Route path="*" element={<NotFoundPage />} /> AL FINAL, SIN ProtectedRoute
```

De vite.config.js — config PWA actual:
```js
VitePWA({
  registerType: 'autoUpdate',
  manifest: {
    name: 'NovAttend', short_name: 'NovAttend',
    theme_color: '#800000', background_color: '#FAFAF8',
    display: 'standalone',
    // FALTAN: start_url, scope, lang
    icons: [...]
  },
  workbox: {
    navigateFallback: '/offline.html',  // CAMBIAR a '/index.html'
    runtimeCaching: [
      // ... google fonts ...
      {
        urlPattern: /^https:\/\/script\.google\.com\/.*/i,  // CAMBIAR regex
        handler: 'NetworkFirst', ...
      }
    ]
  }
})
```

De src/components/ui/Button.jsx — interfaz publica:
```jsx
export default function Button({ variant = 'primary', loading, icon, fullWidth, children, onClick, className, ...rest })
// NotFoundPage usa: <Button variant="primary" onClick={() => navigate('/')}>Volver al inicio</Button>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarea 1: NotFoundPage + ruta catch-all en App.jsx</name>
  <files>src/pages/NotFoundPage.jsx, src/App.jsx, src/tests/NotFoundPage.test.jsx</files>
  <read_first>src/App.jsx, src/components/ui/Button.jsx, src/tests/Button.test.jsx</read_first>
  <behavior>
    - Test: NotFoundPage muestra el heading "404"
    - Test: NotFoundPage muestra el texto "Pagina no encontrada"
    - Test: NotFoundPage tiene un boton "Volver al inicio"
    - Test: Navegar a /ruta-inexistente monta NotFoundPage (ruta catch-all funciona)
  </behavior>
  <action>
    **Paso 1 — Test (RED):**

    Crear `src/tests/NotFoundPage.test.jsx`:
    ```jsx
    import { describe, it, expect, vi } from 'vitest'
    import { render, screen } from '@testing-library/react'
    import userEvent from '@testing-library/user-event'
    import { MemoryRouter } from 'react-router-dom'
    import NotFoundPage from '../pages/NotFoundPage'

    // Mock useNavigate
    const mockNavigate = vi.fn()
    vi.mock('react-router-dom', async () => {
      const actual = await vi.importActual('react-router-dom')
      return { ...actual, useNavigate: () => mockNavigate }
    })

    describe('NotFoundPage', () => {
      beforeEach(() => {
        mockNavigate.mockClear()
      })

      it('muestra el heading 404', () => {
        render(<MemoryRouter><NotFoundPage /></MemoryRouter>)
        expect(screen.getByText('404')).toBeInTheDocument()
      })

      it('muestra el mensaje Pagina no encontrada', () => {
        render(<MemoryRouter><NotFoundPage /></MemoryRouter>)
        expect(screen.getByText('Pagina no encontrada')).toBeInTheDocument()
      })

      it('tiene boton Volver al inicio que navega a /', async () => {
        render(<MemoryRouter><NotFoundPage /></MemoryRouter>)
        const btn = screen.getByRole('button', { name: /volver al inicio/i })
        expect(btn).toBeInTheDocument()
        await userEvent.click(btn)
        expect(mockNavigate).toHaveBeenCalledWith('/')
      })
    })
    ```

    **Paso 2 — Implementacion (GREEN):**

    2a. Crear `src/pages/NotFoundPage.jsx` (per D-03 — branded minima, sin animaciones):
    ```jsx
    import { useNavigate } from 'react-router-dom'
    import Button from '../components/ui/Button.jsx'

    /**
     * Pagina 404 — ruta no encontrada.
     * Branded minima: fondo oscuro, heading gold, boton a login.
     */
    export default function NotFoundPage() {
      const navigate = useNavigate()

      return (
        <div className="min-h-dvh min-h-screen w-full max-w-[430px] mx-auto bg-dark-bg flex flex-col items-center justify-center p-8 box-border">
          <h1 className="font-cinzel text-6xl font-bold text-gold m-0 mb-3">
            404
          </h1>
          <p className="font-montserrat text-sm text-white/60 m-0 mb-8 text-center">
            Pagina no encontrada
          </p>
          <Button variant="primary" onClick={() => navigate('/')}>
            Volver al inicio
          </Button>
        </div>
      )
    }
    ```

    2b. En `src/App.jsx`, agregar import de NotFoundPage despues de la linea de import de DashboardPage:
    ```js
    import NotFoundPage from './pages/NotFoundPage'
    ```

    Y agregar la ruta catch-all como ULTIMA Route dentro de `<Routes>`, DESPUES del Route de /dashboard y ANTES del cierre de `</Routes>`:
    ```jsx
    <Route path="*" element={<NotFoundPage />} />
    ```

    IMPORTANTE: Sin ProtectedRoute — la pagina 404 es publica.
  </action>
  <verify>
    <automated>cd /c/Users/Usuario/Desktop/novattend && npm test -- --run NotFoundPage.test 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/NotFoundPage.jsx existe y contiene `font-cinzel`, `text-gold`, `text-6xl`, texto "404", texto "Pagina no encontrada", texto "Volver al inicio"
    - src/pages/NotFoundPage.jsx tiene menos de 30 lineas
    - src/pages/NotFoundPage.jsx NO contiene `style={{`
    - src/pages/NotFoundPage.jsx tiene JSDoc en la cabecera
    - src/App.jsx contiene `import NotFoundPage from './pages/NotFoundPage'`
    - src/App.jsx contiene `<Route path="*" element={<NotFoundPage />} />`
    - src/App.jsx NO contiene `ProtectedRoute` envolviendo NotFoundPage
    - src/tests/NotFoundPage.test.jsx existe con 3 tests
    - `npm test -- --run NotFoundPage.test` pasa con 0 fallos
  </acceptance_criteria>
  <done>NotFoundPage.jsx implementa D-03 (branded minima). App.jsx tiene ruta catch-all publica. Tests verifican rendering y navegacion.</done>
</task>

<task type="auto">
  <name>Tarea 2: 3 fixes PWA en vite.config.js (navigateFallback, regex, manifest)</name>
  <files>vite.config.js</files>
  <read_first>vite.config.js</read_first>
  <action>
    Aplicar los 3 fixes en `vite.config.js`:

    **Fix PWA-01 — navigateFallback (linea 29):**
    - DE: `navigateFallback: '/offline.html',`
    - A:  `navigateFallback: '/index.html',`

    **Fix PWA-02 — regex de runtime caching (linea 49):**
    - DE: `urlPattern: /^https:\/\/script\.google\.com\/.*/i,`
    - A:  `urlPattern: /^https:\/\/script\.(google|googleusercontent)\.com\/.*/i,`

    **Fix PWA-03 — campos de manifest (despues de `display: 'standalone',` en linea 21):**
    Agregar estas 3 lineas entre `display: 'standalone',` y `icons: [`:
    ```js
    start_url: '/',
    scope: '/',
    lang: 'es',
    ```

    El resultado final del bloque manifest debe ser:
    ```js
    manifest: {
      name: 'NovAttend',
      short_name: 'NovAttend',
      theme_color: '#800000',
      background_color: '#FAFAF8',
      display: 'standalone',
      start_url: '/',
      scope: '/',
      lang: 'es',
      icons: [
        { src: '/logova1.png', sizes: '192x192', type: 'image/png' },
        { src: '/logova1.png', sizes: '512x512', type: 'image/png' }
      ]
    },
    ```

    Despues de aplicar los cambios, ejecutar `npm run build` para verificar que la config es valida y el build completa.
  </action>
  <verify>
    <automated>cd /c/Users/Usuario/Desktop/novattend && npm run build 2>&1 | tail -10 && echo "---" && grep "navigateFallback" vite.config.js && grep "googleusercontent" vite.config.js && grep "start_url" vite.config.js</automated>
  </verify>
  <acceptance_criteria>
    - vite.config.js contiene `navigateFallback: '/index.html'` (no '/offline.html')
    - vite.config.js contiene `googleusercontent` en el regex de urlPattern
    - vite.config.js contiene `start_url: '/'`
    - vite.config.js contiene `scope: '/'`
    - vite.config.js contiene `lang: 'es'`
    - `npm run build` completa sin errores
  </acceptance_criteria>
  <done>PWA configurada correctamente: SPA fallback a index.html, cache captura ambos dominios de Google, manifest tiene los 3 campos requeridos.</done>
</task>

</tasks>

<verification>
```bash
# Verificar NotFoundPage
test -f src/pages/NotFoundPage.jsx && echo "OK: NotFoundPage existe" || echo "FALLO"
grep 'path="\*"' src/App.jsx && echo "OK: ruta catch-all" || echo "FALLO"
grep 'ProtectedRoute.*NotFound' src/App.jsx && echo "FALLO: 404 protegida" || echo "OK: 404 publica"

# Verificar PWA fixes
grep "index.html" vite.config.js && echo "OK: navigateFallback" || echo "FALLO"
grep "googleusercontent" vite.config.js && echo "OK: regex" || echo "FALLO"
grep "start_url" vite.config.js && echo "OK: start_url" || echo "FALLO"
grep "scope" vite.config.js && echo "OK: scope" || echo "FALLO"
grep "lang: 'es'" vite.config.js && echo "OK: lang" || echo "FALLO"

# Suite completa
npm test -- --run && npm run lint && npm run build
```
</verification>

<success_criteria>
- NotFoundPage.jsx existe con heading "404", texto "Pagina no encontrada", boton "Volver al inicio"
- App.jsx tiene ruta catch-all `path="*"` SIN ProtectedRoute
- vite.config.js tiene navigateFallback: '/index.html'
- vite.config.js tiene regex con `(google|googleusercontent)`
- vite.config.js manifest tiene start_url, scope, lang
- Todos los tests pasan, lint limpio, build exitoso
</success_criteria>

<output>
After completion, create `.planning/phases/01-estabilidad-critica/01-03-SUMMARY.md`
</output>
