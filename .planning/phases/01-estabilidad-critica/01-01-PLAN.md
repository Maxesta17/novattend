---
phase: 01-estabilidad-critica
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tailwind.config.js
  - src/components/ui/Button.jsx
  - src/components/ui/ToggleSwitch.jsx
  - src/components/MobileContainer.jsx
  - src/styles/mobile-container.css
  - index.html
  - src/tests/Button.test.jsx
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - COMP-03

must_haves:
  truths:
    - "Button.jsx y ToggleSwitch.jsx usan bg-disabled en lugar de hex hardcodeados"
    - "MobileContainer no tiene bloque <style> inline — el CSS vive en un archivo separado"
    - "index.html tiene lang='es' y titulo 'NovAttend'"
    - "npm audit no reporta vulnerabilidades con fix disponible"
  artifacts:
    - path: "tailwind.config.js"
      provides: "Token disabled en extend.colors"
      contains: "disabled: '#CCCCCC'"
    - path: "src/components/ui/Button.jsx"
      provides: "Variante disabled con token Tailwind"
      contains: "bg-disabled"
    - path: "src/components/ui/ToggleSwitch.jsx"
      provides: "Estado off con token Tailwind"
      contains: "bg-disabled"
    - path: "src/styles/mobile-container.css"
      provides: "Media query de desktop extraida de MobileContainer"
      contains: "@media (min-width: 480px)"
    - path: "index.html"
      provides: "Metadata de idioma y titulo correctos"
      contains: "lang=\"es\""
  key_links:
    - from: "src/components/ui/Button.jsx"
      to: "tailwind.config.js"
      via: "clase bg-disabled definida como token"
      pattern: "bg-disabled"
    - from: "src/components/MobileContainer.jsx"
      to: "src/styles/mobile-container.css"
      via: "import del archivo CSS"
      pattern: "import.*mobile-container\\.css"
---

<objective>
Compliance con CLAUDE.md: eliminar hex hardcodeados, corregir metadata HTML, resolver vulnerabilidades npm.

Purpose: Los 3 hex hardcodeados violan la regla de design system de CLAUDE.md. El lang="en" en index.html es incorrecto para una app en espanol. Las vulnerabilidades de npm son deuda tecnica inmediata.
Output: Token `disabled` en Tailwind, 0 hex hardcodeados en Button/ToggleSwitch/MobileContainer, index.html con lang="es", npm audit limpio.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@tailwind.config.js
@src/components/ui/Button.jsx
@src/components/ui/ToggleSwitch.jsx
@src/components/MobileContainer.jsx
@index.html
@src/tests/Button.test.jsx

<interfaces>
<!-- Tokens Tailwind existentes en tailwind.config.js que el executor necesita conocer -->
<!-- Solo se AGREGA una entrada; no se modifica ninguna existente -->

De tailwind.config.js:
```js
extend: {
  colors: {
    burgundy: '#800000',
    // ... 20+ tokens existentes ...
    'dark-bg': '#111111',
    // AGREGAR AQUI: disabled: '#CCCCCC'
  }
}
```

De Button.jsx linea 32 (valor actual a reemplazar):
```js
disabled: 'bg-[#CCCCCC] text-white cursor-not-allowed hover:translate-y-0',
```

De ToggleSwitch.jsx linea 22 (valor actual a reemplazar):
```js
: 'bg-[#CDCDCD]',
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarea 1: Token disabled + reemplazo de hex en Button y ToggleSwitch</name>
  <files>tailwind.config.js, src/components/ui/Button.jsx, src/components/ui/ToggleSwitch.jsx, src/tests/Button.test.jsx</files>
  <read_first>tailwind.config.js, src/components/ui/Button.jsx, src/components/ui/ToggleSwitch.jsx, src/tests/Button.test.jsx</read_first>
  <behavior>
    - Test: Button con variant="disabled" tiene className que contiene 'bg-disabled' (no 'bg-[#CCCCCC]')
    - Test: ToggleSwitch con checked=false tiene className que contiene 'bg-disabled' (no 'bg-[#CDCDCD]')
  </behavior>
  <action>
    1. En `tailwind.config.js`, agregar `disabled: '#CCCCCC'` al final de `extend.colors` (linea 33, antes del cierre de llaves). Per D-04 — token semantico para elementos deshabilitados.

    2. En `src/components/ui/Button.jsx` linea 32, cambiar:
       - DE: `disabled: 'bg-[#CCCCCC] text-white cursor-not-allowed hover:translate-y-0',`
       - A:  `disabled: 'bg-disabled text-white cursor-not-allowed hover:translate-y-0',`

    3. En `src/components/ui/ToggleSwitch.jsx` linea 22, cambiar:
       - DE: `'bg-[#CDCDCD]'`
       - A:  `'bg-disabled'`

    4. En `src/tests/Button.test.jsx`, agregar un test al final del describe:
       ```jsx
       it('variante disabled usa clase bg-disabled (token Tailwind, no hex)', () => {
         render(<Button variant="disabled">Off</Button>)
         const btn = screen.getByRole('button')
         expect(btn.className).toContain('bg-disabled')
         expect(btn.className).not.toContain('bg-[#')
       })
       ```
  </action>
  <verify>
    <automated>cd /c/Users/Usuario/Desktop/novattend && npm test -- --run Button.test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tailwind.config.js contiene la linea `disabled: '#CCCCCC'` dentro de extend.colors
    - Button.jsx contiene `'bg-disabled` y NO contiene `bg-[#CCCCCC]`
    - ToggleSwitch.jsx contiene `'bg-disabled'` y NO contiene `bg-[#CDCDCD]`
    - Button.test.jsx contiene test 'variante disabled usa clase bg-disabled'
    - `npm test -- --run Button.test` pasa con 0 fallos
  </acceptance_criteria>
  <done>Token disabled existe en tailwind.config.js. Button y ToggleSwitch usan bg-disabled. Test de className verifica ausencia de hex hardcodeados.</done>
</task>

<task type="auto">
  <name>Tarea 2: MobileContainer CSS extraction + index.html metadata + npm audit</name>
  <files>src/components/MobileContainer.jsx, src/styles/mobile-container.css, index.html</files>
  <read_first>src/components/MobileContainer.jsx, index.html, src/styles/animations.css</read_first>
  <action>
    1. Crear `src/styles/mobile-container.css` con el contenido del bloque `<style>` de MobileContainer.jsx. Agregar comentario documentando que `#111111` corresponde al token `dark-bg` de tailwind.config.js:

       ```css
       /* Media query de desktop para MobileContainer.
          El valor #111111 corresponde al token dark-bg de tailwind.config.js.
          Tailwind 3 no genera CSS vars para extend.colors, por lo que
          el hex directo es la unica opcion dentro de CSS puro. */
       @media (min-width: 480px) {
         #root {
           background-color: #111111;
           display: flex;
           align-items: center;
           justify-content: center;
           padding: 20px 0;
           min-height: 100vh;
         }
         .mobile-container {
           max-width: 430px;
           min-height: 100vh;
           border-radius: 40px;
           box-shadow: 0 20px 60px rgba(0, 0, 0, 0.5);
           overflow: hidden;
         }
       }
       ```

    2. En `src/components/MobileContainer.jsx`, reemplazar TODO el contenido por:

       ```jsx
       import '../styles/mobile-container.css'

       /**
        * Contenedor mobile-first con frame de dispositivo en desktop.
        * Los estilos de media query desktop estan en src/styles/mobile-container.css.
        * @param {object} props
        * @param {React.ReactNode} props.children - Contenido de la pagina
        */
       export default function MobileContainer({ children }) {
         return (
           <div className="mobile-container w-full min-h-screen">
             {children}
           </div>
         )
       }
       ```

    3. En `index.html` linea 2, cambiar:
       - DE: `<html lang="en">`
       - A:  `<html lang="es">`

    4. En `index.html` linea 8, cambiar:
       - DE: `<title>novattend</title>`
       - A:  `<title>NovAttend</title>`

    5. En `index.html`, agregar debajo de la linea de viewport meta (linea 7):
       `<meta name="theme-color" content="#800000">`

    6. Ejecutar `npm audit fix` (sin --force). Luego ejecutar `npm run build` y `npm test` para confirmar que nada se rompio.
  </action>
  <verify>
    <automated>cd /c/Users/Usuario/Desktop/novattend && npm run lint 2>&1 | tail -5 && npm test -- --run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/styles/mobile-container.css existe y contiene `@media (min-width: 480px)` y `background-color: #111111`
    - src/components/MobileContainer.jsx contiene `import '../styles/mobile-container.css'` y NO contiene `<style>`
    - MobileContainer.jsx tiene menos de 20 lineas
    - index.html contiene `lang="es"` (no `lang="en"`)
    - index.html contiene `<title>NovAttend</title>` (no `novattend`)
    - index.html contiene `<meta name="theme-color" content="#800000">`
    - `npm run lint` pasa con 0 errores
    - `npm test` pasa con 0 fallos
    - `npm run build` completa sin errores
  </acceptance_criteria>
  <done>MobileContainer usa archivo CSS externo (sin bloque style inline). index.html tiene lang="es", titulo correcto y theme-color. npm audit fix ejecutado sin romper build ni tests.</done>
</task>

</tasks>

<verification>
```bash
# Verificar cero hex hardcodeados en componentes ui/
grep -rn "bg-\[#" src/components/ui/ && echo "FALLO: hex hardcodeados encontrados" || echo "OK: cero hex en ui/"

# Verificar token disabled en tailwind config
grep "disabled:" tailwind.config.js && echo "OK: token disabled existe" || echo "FALLO: token disabled faltante"

# Verificar lang en index.html
grep 'lang="es"' index.html && echo "OK: lang=es" || echo "FALLO: lang incorrecto"

# Verificar MobileContainer sin style tag
grep "<style>" src/components/MobileContainer.jsx && echo "FALLO: style tag persiste" || echo "OK: sin style tag"

# Suite completa
npm test -- --run && npm run lint && npm run build
```
</verification>

<success_criteria>
- Token `disabled: '#CCCCCC'` existe en tailwind.config.js
- 0 instancias de `bg-[#` en src/components/ui/
- MobileContainer.jsx no contiene `<style>` — CSS esta en archivo separado
- index.html tiene lang="es", titulo NovAttend, meta theme-color
- npm audit fix ejecutado
- Todos los tests pasan, lint limpio, build exitoso
</success_criteria>

<output>
After completion, create `.planning/phases/01-estabilidad-critica/01-01-SUMMARY.md`
</output>
