---
phase: 01-estabilidad-critica
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/api.js
  - src/components/ui/ErrorBanner.jsx
  - src/hooks/useStudents.js
  - src/pages/AttendancePage.jsx
  - src/pages/SavedPage.jsx
  - src/tests/api.test.jsx
  - src/tests/ErrorBanner.test.jsx
  - src/tests/SavedPage.test.jsx
autonomous: true
requirements:
  - ERR-01
  - ERR-02
  - ERR-03

must_haves:
  truths:
    - "Cuando la API devuelve HTTP 4xx/5xx, apiGet y apiPost lanzan Error con mensaje descriptivo en lugar de fallar en res.json()"
    - "AttendancePage muestra un banner de error visible cuando falla guardar asistencia"
    - "AttendancePage muestra un banner de error visible cuando falla cargar alumnos"
    - "SavedPage muestra el resumen correctamente cuando present === 0 (no redirige)"
  artifacts:
    - path: "src/services/api.js"
      provides: "Verificacion res.ok antes de parsear JSON"
      contains: "if (!res.ok)"
    - path: "src/components/ui/ErrorBanner.jsx"
      provides: "Componente banner de error reutilizable"
      exports: ["ErrorBanner"]
      contains: "role=\"alert\""
    - path: "src/hooks/useStudents.js"
      provides: "Campo loadError expuesto por el hook"
      contains: "loadError"
    - path: "src/pages/AttendancePage.jsx"
      provides: "Integracion de ErrorBanner para errores de carga y guardado"
      contains: "ErrorBanner"
    - path: "src/pages/SavedPage.jsx"
      provides: "Fix del bug present===0"
      contains: "state.present === undefined"
  key_links:
    - from: "src/services/api.js"
      to: "src/hooks/useStudents.js"
      via: "getAlumnos lanza Error que useStudents captura en loadError"
      pattern: "catch.*loadError"
    - from: "src/hooks/useStudents.js"
      to: "src/pages/AttendancePage.jsx"
      via: "loadError expuesto y consumido por AttendancePage"
      pattern: "loadError"
    - from: "src/components/ui/ErrorBanner.jsx"
      to: "src/pages/AttendancePage.jsx"
      via: "import y render con message={saveError} y message={loadError}"
      pattern: "ErrorBanner"
---

<objective>
Manejo de errores robusto: API valida HTTP responses, errores son visibles al usuario, SavedPage no tiene bug logico con present=0.

Purpose: Actualmente la API falla silenciosamente con HTTP errors (intenta parsear HTML como JSON), AttendancePage tiene un banner ad-hoc para errores de guardado pero no para errores de carga, y SavedPage redirige incorrectamente cuando todos los alumnos estan ausentes (present=0 es falsy).
Output: api.js con check res.ok, ErrorBanner.jsx reutilizable, useStudents con loadError, AttendancePage con banners de error, SavedPage con bug corregido.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/services/api.js
@src/hooks/useStudents.js
@src/pages/AttendancePage.jsx
@src/pages/SavedPage.jsx
@src/tests/api.test.jsx

<interfaces>
<!-- Interfaces clave que el executor necesita -->

De src/services/api.js — funciones base a modificar:
```js
async function apiGet(action, params = {}) {
  // ... construye URL ...
  const res = await fetch(url.toString())
  const json = await res.json()  // <-- FALTA check res.ok ANTES de esta linea
  // ...
}

async function apiPost(action, body = {}) {
  // ... fetch con POST ...
  const json = await res.json()  // <-- FALTA check res.ok ANTES de esta linea
  // ...
}
```

De src/hooks/useStudents.js — return actual (agregar loadError):
```js
return {
  students, loadingStudents, selectedGroup,
  setSelectedGroup: handleGroupChange,
  toggleStudent, toggleAll,
  presentCount, absentCount, attendancePercent,
  // AGREGAR: loadError
}
```

De src/pages/AttendancePage.jsx — destructuring del hook (agregar loadError):
```js
const {
  students, loadingStudents, selectedGroup, setSelectedGroup,
  toggleStudent, toggleAll, presentCount, absentCount, attendancePercent,
  // AGREGAR: loadError
} = useStudents(convocatoria, profesorId)
```

De src/pages/AttendancePage.jsx lineas 161-165 — banner ad-hoc a REEMPLAZAR:
```jsx
{saveError && (
  <div className="mb-2 px-3 py-2 bg-error/10 border border-error/30 rounded-lg text-error text-xs font-montserrat text-center">
    {saveError}
  </div>
)}
```

De src/pages/SavedPage.jsx linea 12 — condicion buggy:
```js
if (!state || !state.present || state.total === undefined) {
//           ^^^^^^^^^^^^^^ -- 0 es falsy, redirige cuando no deberia
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarea 1: Fix res.ok en api.js + ErrorBanner.jsx + SavedPage bug</name>
  <files>src/services/api.js, src/components/ui/ErrorBanner.jsx, src/pages/SavedPage.jsx, src/tests/api.test.jsx, src/tests/ErrorBanner.test.jsx, src/tests/SavedPage.test.jsx</files>
  <read_first>src/services/api.js, src/pages/SavedPage.jsx, src/tests/api.test.jsx, src/tests/Button.test.jsx, src/components/ui/Button.jsx</read_first>
  <behavior>
    - Test api: apiGet lanza "Error HTTP 500: Internal Server Error" cuando fetch devuelve res.ok=false con status 500
    - Test api: apiPost lanza "Error HTTP 403: Forbidden" cuando fetch devuelve res.ok=false con status 403
    - Test ErrorBanner: renderiza mensaje cuando message tiene contenido
    - Test ErrorBanner: no renderiza nada cuando message es null
    - Test ErrorBanner: no renderiza nada cuando message es string vacio
    - Test ErrorBanner: llama onDismiss al click en boton X
    - Test ErrorBanner: tiene role="alert" en el div raiz
    - Test ErrorBanner: no muestra boton X cuando onDismiss no se pasa
    - Test SavedPage: no redirige cuando present===0 (muestra la pagina con 0 presentes)
    - Test SavedPage: redirige cuando state es null (no tiene datos)
  </behavior>
  <action>
    **Paso 1 — Tests (RED):**

    1a. En `src/tests/api.test.jsx`, agregar 2 tests al final del bloque `// --- apiGet ---`:
    ```jsx
    it('apiGet lanza error descriptivo cuando res.ok es false (HTTP 500)', async () => {
      global.fetch.mockResolvedValue({
        ok: false,
        status: 500,
        statusText: 'Internal Server Error',
      })
      await expect(getConvocatorias()).rejects.toThrow('Error HTTP 500: Internal Server Error')
    })
    ```

    Y al final del bloque `// --- apiPost ---`:
    ```jsx
    it('apiPost lanza error descriptivo cuando res.ok es false (HTTP 403)', async () => {
      global.fetch.mockResolvedValue({
        ok: false,
        status: 403,
        statusText: 'Forbidden',
      })
      await expect(guardarAsistencia({})).rejects.toThrow('Error HTTP 403: Forbidden')
    })
    ```

    IMPORTANTE: Tambien hay que agregar `ok: true` a TODOS los mocks de fetch existentes que simulan respuestas exitosas. Actualmente los mocks no tienen `ok` property — despues del fix esto causaria que los tests fallen porque `res.ok` seria `undefined` (falsy). Buscar cada `mockResolvedValue({` en el archivo y agregar `ok: true,` como primera propiedad del objeto.

    1b. Crear `src/tests/ErrorBanner.test.jsx`:
    ```jsx
    import { describe, it, expect, vi } from 'vitest'
    import { render, screen } from '@testing-library/react'
    import userEvent from '@testing-library/user-event'
    import ErrorBanner from '../components/ui/ErrorBanner'

    describe('ErrorBanner', () => {
      it('renderiza el mensaje cuando message tiene contenido', () => {
        render(<ErrorBanner message="Error de red" />)
        expect(screen.getByRole('alert')).toBeInTheDocument()
        expect(screen.getByText('Error de red')).toBeInTheDocument()
      })

      it('no renderiza nada cuando message es null', () => {
        const { container } = render(<ErrorBanner message={null} />)
        expect(container.firstChild).toBeNull()
      })

      it('no renderiza nada cuando message es string vacio', () => {
        const { container } = render(<ErrorBanner message="" />)
        expect(container.firstChild).toBeNull()
      })

      it('llama onDismiss al click en boton X', async () => {
        const handleDismiss = vi.fn()
        render(<ErrorBanner message="Error" onDismiss={handleDismiss} />)
        await userEvent.click(screen.getByLabelText('Cerrar error'))
        expect(handleDismiss).toHaveBeenCalledOnce()
      })

      it('tiene role="alert" en el div raiz', () => {
        render(<ErrorBanner message="Error" />)
        expect(screen.getByRole('alert')).toBeInTheDocument()
      })

      it('no muestra boton X cuando onDismiss no se pasa', () => {
        render(<ErrorBanner message="Error" />)
        expect(screen.queryByLabelText('Cerrar error')).toBeNull()
      })
    })
    ```

    1c. Crear `src/tests/SavedPage.test.jsx`:
    ```jsx
    import { describe, it, expect, vi } from 'vitest'
    import { render, screen } from '@testing-library/react'
    import { MemoryRouter } from 'react-router-dom'

    // Mock useNavigate
    const mockNavigate = vi.fn()
    vi.mock('react-router-dom', async () => {
      const actual = await vi.importActual('react-router-dom')
      return {
        ...actual,
        useNavigate: () => mockNavigate,
        useLocation: vi.fn(),
      }
    })

    import { useLocation } from 'react-router-dom'
    import SavedPage from '../pages/SavedPage'

    describe('SavedPage', () => {
      beforeEach(() => {
        mockNavigate.mockClear()
      })

      it('no redirige cuando present es 0 (muestra la pagina)', () => {
        useLocation.mockReturnValue({
          state: { present: 0, total: 10, group: 'G1', convocatoria: null },
        })
        render(<SavedPage />)
        expect(mockNavigate).not.toHaveBeenCalled()
        expect(screen.getByText('Asistencia guardada')).toBeInTheDocument()
      })

      it('redirige a /attendance cuando state es null', () => {
        useLocation.mockReturnValue({ state: null })
        render(<SavedPage />)
        expect(mockNavigate).toHaveBeenCalledWith('/attendance')
      })

      it('redirige cuando present es undefined', () => {
        useLocation.mockReturnValue({
          state: { total: 10, group: 'G1' },
        })
        render(<SavedPage />)
        expect(mockNavigate).toHaveBeenCalledWith('/attendance')
      })
    })
    ```

    **Paso 2 — Implementacion (GREEN):**

    2a. En `src/services/api.js`, funcion `apiGet` (linea 27), agregar ANTES de `const json = await res.json()`:
    ```js
    if (!res.ok) {
      throw new Error(`Error HTTP ${res.status}: ${res.statusText}`)
    }
    ```

    En funcion `apiPost` (linea 44), agregar ANTES de `const json = await res.json()`:
    ```js
    if (!res.ok) {
      throw new Error(`Error HTTP ${res.status}: ${res.statusText}`)
    }
    ```

    2b. Crear `src/components/ui/ErrorBanner.jsx` con el contenido exacto del RESEARCH.md Code Examples (per D-01 y D-02):
    ```jsx
    /**
     * Banner de error inline reutilizable.
     * @param {object} props
     * @param {string|null} props.message - Mensaje de error. Si es null/empty, no renderiza.
     * @param {function} [props.onDismiss] - Callback al cerrar. Si no se pasa, no muestra boton X.
     */
    export default function ErrorBanner({ message, onDismiss }) {
      if (!message) return null

      const classes = [
        'flex items-center gap-2 mb-2 px-3 py-2',
        'bg-error-soft border border-error/30 rounded-lg',
        'text-error text-xs font-montserrat',
      ].join(' ')

      return (
        <div className={classes} role="alert">
          <span className="flex-1">{message}</span>
          {onDismiss && (
            <button
              type="button"
              onClick={onDismiss}
              className="shrink-0 text-error/70 hover:text-error font-bold text-sm leading-none p-1"
              aria-label="Cerrar error"
            >
              ×
            </button>
          )}
        </div>
      )
    }
    ```

    2c. En `src/pages/SavedPage.jsx` linea 12, cambiar la condicion del useEffect:
    - DE: `if (!state || !state.present || state.total === undefined) {`
    - A:  `if (!state || state.present === undefined || state.total === undefined) {`

    SOLO cambiar esta linea. No tocar la linea 17 (`if (!state) return null`).

    **Paso 3 — Verificar todos los tests pasan.**
  </action>
  <verify>
    <automated>cd /c/Users/Usuario/Desktop/novattend && npm test -- --run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/services/api.js contiene `if (!res.ok)` en dos lugares (antes de cada `res.json()`)
    - src/services/api.js contiene `throw new Error(`Error HTTP ${res.status}: ${res.statusText}`)`
    - src/components/ui/ErrorBanner.jsx existe y contiene `role="alert"` y `aria-label="Cerrar error"`
    - src/components/ui/ErrorBanner.jsx contiene `if (!message) return null`
    - src/components/ui/ErrorBanner.jsx NO contiene `style={{`
    - src/pages/SavedPage.jsx contiene `state.present === undefined` (no `!state.present`)
    - src/tests/api.test.jsx contiene test 'apiGet lanza error descriptivo cuando res.ok es false'
    - src/tests/api.test.jsx contiene test 'apiPost lanza error descriptivo cuando res.ok es false'
    - src/tests/ErrorBanner.test.jsx existe con 6 tests
    - src/tests/SavedPage.test.jsx existe con 3 tests
    - `npm test -- --run` pasa con 0 fallos
  </acceptance_criteria>
  <done>api.js verifica res.ok. ErrorBanner.jsx es componente puro con role=alert. SavedPage no redirige con present=0. Tests cubren los 3 requisitos.</done>
</task>

<task type="auto">
  <name>Tarea 2: Integrar ErrorBanner en AttendancePage + loadError en useStudents</name>
  <files>src/hooks/useStudents.js, src/pages/AttendancePage.jsx</files>
  <read_first>src/hooks/useStudents.js, src/pages/AttendancePage.jsx, src/components/ui/ErrorBanner.jsx</read_first>
  <action>
    **Paso 1 — useStudents.js: exponer loadError**

    1a. Agregar estado `loadError` al hook. Despues de la linea `const [loadingStudents, setLoadingStudents] = useState(true)` (linea 43), agregar:
    ```js
    const [loadError, setLoadError] = useState(null)
    ```

    1b. En la funcion `loadStudents` (callback), en el bloque catch (linea 69), cambiar:
    - DE: `catch { setStudents([]) }`
    - A:
    ```js
    catch (err) {
      setStudents([])
      setLoadError(err.message || 'No se pudieron cargar los alumnos. Revisa tu conexion.')
    }
    ```

    1c. En el useEffect `init` (linea 76), en el bloque catch (linea 97), cambiar:
    - DE:
    ```js
    catch {
      if (!cancelled) {
        setStudents([])
        setLoadingStudents(false)
      }
    }
    ```
    - A:
    ```js
    catch (err) {
      if (!cancelled) {
        setStudents([])
        setLoadError(err.message || 'No se pudieron cargar los alumnos. Revisa tu conexion.')
        setLoadingStudents(false)
      }
    }
    ```

    1d. En `loadStudents`, ANTES del try (antes de `setLoadingStudents(true)` en linea 63), agregar:
    ```js
    setLoadError(null)
    ```

    1e. En el return del hook (linea 143), agregar `loadError` al objeto retornado:
    ```js
    return {
      students,
      loadingStudents,
      loadError,
      selectedGroup,
      setSelectedGroup: handleGroupChange,
      toggleStudent,
      toggleAll,
      presentCount,
      absentCount,
      attendancePercent,
    }
    ```

    **Paso 2 — AttendancePage.jsx: integrar ErrorBanner**

    2a. Agregar import de ErrorBanner. Despues de la linea de import de Button (linea 12), agregar:
    ```js
    import ErrorBanner from '../components/ui/ErrorBanner.jsx'
    ```

    2b. En el destructuring de useStudents (linea 30), agregar `loadError`:
    ```js
    const {
      students,
      loadingStudents,
      loadError,
      selectedGroup,
      setSelectedGroup,
      toggleStudent,
      toggleAll,
      presentCount,
      absentCount,
      attendancePercent,
    } = useStudents(convocatoria, profesorId)
    ```

    2c. Agregar ErrorBanner de carga despues del ProgressBar (linea 114), antes del div.flex-1:
    ```jsx
    <ErrorBanner
      message={loadError}
      onDismiss={() => {/* loadError se limpia automaticamente al cambiar de grupo */}}
    />
    ```

    Nota: Envolver en `<div className="px-4">` para mantener padding consistente:
    ```jsx
    {loadError && (
      <div className="px-4">
        <ErrorBanner message={loadError} />
      </div>
    )}
    ```

    2d. Reemplazar el banner ad-hoc de saveError (lineas 161-165 del bloque fixed bottom). Cambiar:
    - DE:
    ```jsx
    {saveError && (
      <div className="mb-2 px-3 py-2 bg-error/10 border border-error/30 rounded-lg text-error text-xs font-montserrat text-center">
        {saveError}
      </div>
    )}
    ```
    - A:
    ```jsx
    <ErrorBanner message={saveError} onDismiss={() => setSaveError(null)} />
    ```
  </action>
  <verify>
    <automated>cd /c/Users/Usuario/Desktop/novattend && npm test -- --run 2>&1 | tail -20 && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/hooks/useStudents.js contiene `const [loadError, setLoadError] = useState(null)`
    - src/hooks/useStudents.js return contiene `loadError`
    - src/hooks/useStudents.js contiene `setLoadError(err.message` en al menos 2 bloques catch
    - src/pages/AttendancePage.jsx contiene `import ErrorBanner from '../components/ui/ErrorBanner.jsx'`
    - src/pages/AttendancePage.jsx contiene `loadError` en el destructuring de useStudents
    - src/pages/AttendancePage.jsx contiene `<ErrorBanner message={saveError}` (reemplazando el div ad-hoc)
    - src/pages/AttendancePage.jsx contiene `<ErrorBanner message={loadError}`
    - src/pages/AttendancePage.jsx NO contiene `bg-error/10` (el banner ad-hoc fue eliminado)
    - `npm test -- --run` pasa con 0 fallos
    - `npm run lint` pasa con 0 errores
  </acceptance_criteria>
  <done>useStudents expone loadError. AttendancePage muestra ErrorBanner para errores de carga y guardado. El banner ad-hoc fue reemplazado por el componente reutilizable.</done>
</task>

</tasks>

<verification>
```bash
# Verificar res.ok check en api.js
grep -n "if (!res.ok)" src/services/api.js | wc -l  # Debe ser 2

# Verificar ErrorBanner existe y es puro
grep "style={{" src/components/ui/ErrorBanner.jsx && echo "FALLO: inline style" || echo "OK: sin inline styles"
grep 'role="alert"' src/components/ui/ErrorBanner.jsx && echo "OK: role=alert" || echo "FALLO: falta role=alert"

# Verificar SavedPage fix
grep "state.present === undefined" src/pages/SavedPage.jsx && echo "OK: fix aplicado" || echo "FALLO: bug persiste"

# Verificar integracion ErrorBanner en AttendancePage
grep "ErrorBanner" src/pages/AttendancePage.jsx | wc -l  # Debe ser >= 3 (import + 2 usos)
grep "bg-error/10" src/pages/AttendancePage.jsx && echo "FALLO: banner ad-hoc persiste" || echo "OK: banner ad-hoc eliminado"

# Verificar useStudents expone loadError
grep "loadError" src/hooks/useStudents.js | wc -l  # Debe ser >= 4

# Suite completa
npm test -- --run && npm run lint
```
</verification>

<success_criteria>
- api.js tiene 2 checks de `if (!res.ok)` — uno en apiGet y uno en apiPost
- ErrorBanner.jsx existe en src/components/ui/, es componente puro, tiene role="alert"
- AttendancePage usa ErrorBanner para saveError y loadError (el banner ad-hoc de bg-error/10 ya no existe)
- useStudents expone loadError y lo setea en los bloques catch
- SavedPage usa `state.present === undefined` en lugar de `!state.present`
- Todos los tests pasan incluyendo los nuevos (api res.ok, ErrorBanner, SavedPage)
- Lint limpio
</success_criteria>

<output>
After completion, create `.planning/phases/01-estabilidad-critica/01-02-SUMMARY.md`
</output>
