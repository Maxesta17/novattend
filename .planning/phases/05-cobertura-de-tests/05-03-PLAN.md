---
phase: 05-cobertura-de-tests
plan: "03"
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - src/tests/AttendancePage.test.jsx
  - src/tests/DashboardPage.test.jsx
autonomous: true
requirements: [TEST-02, TEST-05]
must_haves:
  truths:
    - "AttendancePage renderiza nombre del profesor, StatCards, lista de alumnos y boton de guardar"
    - "DashboardPage renderiza titulo Dashboard, StatCards con datos, y estado de error con boton Reintentar"
    - "La cobertura total alcanza >= 60% verificada por npm run test:coverage"
  artifacts:
    - path: "src/tests/AttendancePage.test.jsx"
      provides: "Tests de integracion de la pagina principal del teacher"
      min_lines: 60
    - path: "src/tests/DashboardPage.test.jsx"
      provides: "Tests de integracion de la pagina del CEO"
      min_lines: 60
  key_links:
    - from: "src/tests/AttendancePage.test.jsx"
      to: "src/pages/AttendancePage.jsx"
      via: "render + MemoryRouter + mocked services"
      pattern: "import AttendancePage"
    - from: "src/tests/DashboardPage.test.jsx"
      to: "src/pages/DashboardPage.jsx"
      via: "render + MemoryRouter + mocked useDashboard"
      pattern: "import DashboardPage"
    - from: "npm run test:coverage"
      to: "vite.config.js thresholds"
      via: "threshold enforcement >= 60%"
      pattern: "thresholds.*60"
---

<objective>
Escribir tests de integracion para AttendancePage y DashboardPage — las dos paginas principales de la app — y verificar que la cobertura total alcanza >= 60%.

Purpose: Cierra los ultimos gaps de cobertura criticos (TEST-02) y verifica el objetivo final del threshold (TEST-05).
Output: 2 archivos de test de paginas + verificacion de cobertura verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cobertura-de-tests/05-CONTEXT.md
@.planning/phases/05-cobertura-de-tests/05-RESEARCH.md
@.planning/codebase/TESTING.md

# Plan 01 SUMMARY needed: infrastructure must exist
@.planning/phases/05-cobertura-de-tests/05-01-SUMMARY.md

<interfaces>
<!-- Fuente de verdad: src/pages/AttendancePage.jsx -->
<!-- Dependencias a mockear:
  - react-router-dom: useNavigate, useLocation (location.state.convocatoria)
  - ../services/api: guardarAsistencia
  - ../config/api: isApiEnabled
  - useStudents hook se ejecuta real pero con API mocked (per D-05)
  - sessionStorage: user JSON con { username, name, role }
-->
<!-- Renderiza: PageHeader(title=nombre profesor), GroupTabs(GROUPS), StatCard x3 (Presentes/Ausentes/Asistencia), StudentRow x N, Button(Guardar asistencia) -->

<!-- Fuente de verdad: src/pages/DashboardPage.jsx -->
<!-- Dependencias a mockear:
  - react-router-dom: useNavigate
  - ../hooks/useDashboard.js: mock completo del hook (per D-05 excepcion: hook ya testeado)
  - sessionStorage: user JSON con { username, name, role: 'ceo' }
-->
<!-- Renderiza: PageHeader(title="Dashboard"), ConvocatoriaSelector, StatCard x3, SearchInput, TeacherCard x N -->
<!-- Estados: loading -> DashboardSkeleton | error -> error UI + Reintentar | datos -> pagina completa -->

<!-- useDashboard return shape (para mock):
{
  convocatorias: Array,
  convocatoria: Object|null,
  reload: () => void,
  teachers: Array,
  loading: boolean,
  error: string|null,
  expandedTeacher: string|null,
  searchQuery: string,
  setSearchQuery: (q) => void,
  selectedStudent: Object|null,
  setSelectedStudent: (s) => void,
  showAlertPopup: boolean,
  handleAlertClick: () => void,
  handleAlertClose: () => void,
  handleStudentClose: () => void,
  handleClear: () => void,
  handleTeacherToggle: (id) => void,
  handleConvChange: (conv) => void,
  totalStudents: number,
  globalAttendance: number,
  alertStudents: Array,
  searchResults: Array,
}
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tests de integracion de AttendancePage</name>
  <files>src/tests/AttendancePage.test.jsx</files>
  <read_first>
    - src/pages/AttendancePage.jsx (implementacion completa de la pagina)
    - src/hooks/useStudents.js (hook que se ejecuta real con API mockeada)
    - src/tests/LoginPage.test.jsx (patron de mock de react-router-dom + servicios)
    - src/tests/ConvocatoriaPage.test.jsx (patron de test de pagina con navigation state)
  </read_first>
  <behavior>
    - Test 1: Renderiza el nombre del profesor leido de sessionStorage
    - Test 2: Muestra las 3 StatCards (Presentes, Ausentes, Asistencia)
    - Test 3: Renderiza la lista de alumnos del grupo G1 (mock data, 12 nombres)
    - Test 4: El boton "Guardar asistencia" existe y muestra "0/12" inicialmente (0 presentes)
    - Test 5: Muestra nombre de la convocatoria en el subtitulo
    - Test 6: Redirige a / si no hay sesion de usuario (sessionStorage vacio)
  </behavior>
  <action>
Crear `src/tests/AttendancePage.test.jsx`.

Estrategia de mocking (per D-05 — mock a nivel de servicio API, hooks reales ejecutan):
```jsx
const mockNavigate = vi.fn()
vi.mock('react-router-dom', async () => {
  const actual = await vi.importActual('react-router-dom')
  return {
    ...actual,
    useNavigate: () => mockNavigate,
    useLocation: () => ({
      state: { convocatoria: { id: 'conv-1', nombre: 'Enero 2026' } },
    }),
  }
})

vi.mock('../services/api', () => ({
  getAlumnos: vi.fn(),
  guardarAsistencia: vi.fn(),
}))

vi.mock('../config/api', () => ({
  isApiEnabled: vi.fn(() => false),
}))
```

Import de la pagina DESPUES de los vi.mock (hoisting de Vitest):
```jsx
import AttendancePage from '../pages/AttendancePage'
```

Helper de render:
```jsx
function renderPage() {
  sessionStorage.setItem('user', JSON.stringify({
    username: 'samuel', name: 'Samuel', role: 'teacher'
  }))
  return render(<MemoryRouter><AttendancePage /></MemoryRouter>)
}
```

CRITICO: El hook useStudents se ejecuta REAL con isApiEnabled=false. Carga MOCK_GROUPS.G1 = 12 alumnos. Usar `waitFor` para esperar que `loadingStudents` sea false y la lista de alumnos aparezca.

Para el test de "no sesion redirige": NO llamar sessionStorage.setItem. AttendancePage usa useMemo para parsear sessionStorage. Con sesion vacia, `sessionUser` es null y el nombre del profesor sera "Profesor". No redirige por si mismo (eso lo hace ProtectedRoute), pero verificar que muestra "Profesor" como fallback.

NOTA sobre el boton Guardar: El texto es dinamico: `Guardar asistencia · {presentCount}/{totalCount}`. Inicialmente: "Guardar asistencia \u00b7 0/12". Usar regex `getByRole('button', { name: /Guardar asistencia/i })`.

Descripciones en espanol. beforeEach con sessionStorage.clear(), mockNavigate.mockClear(), vi.clearAllMocks().

Mantener bajo 150 lineas.
  </action>
  <verify>
    <automated>npx vitest run src/tests/AttendancePage.test.jsx</automated>
  </verify>
  <acceptance_criteria>
    - src/tests/AttendancePage.test.jsx existe y tiene >= 5 tests
    - Contiene `vi.mock('../services/api'` con getAlumnos y guardarAsistencia
    - Contiene `vi.mock('../config/api'` con isApiEnabled
    - Contiene `vi.mock('react-router-dom'` con useNavigate y useLocation mockeados
    - Contiene `sessionStorage.setItem('user', JSON.stringify(` en helper de render
    - Contiene `waitFor` para esperar carga asincrona de alumnos
    - Contiene asercion de texto 'Presentes' y 'Ausentes'
    - Contiene `getByRole('button', { name: /Guardar asistencia/i })` o equivalente
    - Contiene asercion del nombre 'Samuel' del profesor
    - `npx vitest run src/tests/AttendancePage.test.jsx` pasa con 0 failures
    - Archivo tiene menos de 250 lineas
  </acceptance_criteria>
  <done>AttendancePage tiene tests de integracion que verifican renderizado con datos mock, StatCards, lista de alumnos, y boton de guardar.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tests de integracion de DashboardPage</name>
  <files>src/tests/DashboardPage.test.jsx</files>
  <read_first>
    - src/pages/DashboardPage.jsx (implementacion completa de la pagina)
    - src/hooks/useDashboard.js (interfaz del hook — para construir el mock completo)
    - src/tests/useDashboard.test.jsx (patron existente de mock de este hook)
    - src/tests/LoginPage.test.jsx (patron de mock de react-router-dom)
  </read_first>
  <behavior>
    - Test 1: Renderiza titulo "Dashboard" con datos cargados
    - Test 2: Muestra el total de alumnos y porcentaje de asistencia global en StatCards
    - Test 3: Muestra estado de error con mensaje y boton "Reintentar"
    - Test 4: Muestra DashboardSkeleton cuando loading=true
    - Test 5: Renderiza TeacherCard para cada profesor en teachers
    - Test 6: Boton Reintentar llama reload() del hook
  </behavior>
  <action>
Crear `src/tests/DashboardPage.test.jsx`.

Estrategia de mocking: Mockear `useDashboard` completo (per D-05 excepcion — el hook ya tiene su propio test). Esto aisla la pagina y permite controlar cada estado:

```jsx
const mockNavigate = vi.fn()
vi.mock('react-router-dom', async () => {
  const actual = await vi.importActual('react-router-dom')
  return { ...actual, useNavigate: () => mockNavigate }
})

vi.mock('../hooks/useDashboard.js', () => ({
  default: vi.fn(),
}))

import DashboardPage from '../pages/DashboardPage'
import useDashboard from '../hooks/useDashboard.js'
```

Crear un objeto MOCK_DASHBOARD_STATE completo con TODAS las keys que useDashboard retorna:
```jsx
const MOCK_DASHBOARD_STATE = {
  convocatorias: [{ id: 'c1', nombre: 'Enero 2026' }],
  convocatoria: { id: 'c1', nombre: 'Enero 2026' },
  reload: vi.fn(),
  teachers: [
    {
      id: 'prof-1',
      name: 'Samuel',
      initial: 'S',
      groups: [{
        id: 'G1-prof-1',
        number: 1,
        students: [
          { id: 'a1', name: 'Ana Garcia', weekly: 90, biweekly: 85, monthly: 88 },
        ],
      }],
    },
  ],
  loading: false,
  error: null,
  expandedTeacher: null,
  searchQuery: '',
  setSearchQuery: vi.fn(),
  selectedStudent: null,
  setSelectedStudent: vi.fn(),
  showAlertPopup: false,
  handleAlertClick: vi.fn(),
  handleAlertClose: vi.fn(),
  handleStudentClose: vi.fn(),
  handleClear: vi.fn(),
  handleTeacherToggle: vi.fn(),
  handleConvChange: vi.fn(),
  totalStudents: 48,
  globalAttendance: 83,
  alertStudents: [],
  searchResults: [],
}
```

En beforeEach:
```jsx
useDashboard.mockReturnValue(MOCK_DASHBOARD_STATE)
sessionStorage.setItem('user', JSON.stringify({ username: 'admin', name: 'Admin', role: 'ceo' }))
```

Tests de estado de carga:
```jsx
it('muestra skeleton mientras carga', () => {
  useDashboard.mockReturnValue({ ...MOCK_DASHBOARD_STATE, loading: true })
  renderPage()
  // DashboardSkeleton no tiene texto "Dashboard" — verificar que NO aparece "Dashboard"
  expect(screen.queryByText('Dashboard')).not.toBeInTheDocument()
})
```

Tests de estado de error:
```jsx
it('muestra error con boton Reintentar', () => {
  useDashboard.mockReturnValue({ ...MOCK_DASHBOARD_STATE, loading: false, error: 'Error de red' })
  renderPage()
  expect(screen.getByText('Error de red')).toBeInTheDocument()
  expect(screen.getByRole('button', { name: /Reintentar/i })).toBeInTheDocument()
})
```

NOTA: El boton Reintentar en DashboardPage.jsx es un `<button>` con className pero sin role explicito — getByRole('button') lo detecta por ser <button>. El texto es "Reintentar".

Para verificar que click en Reintentar llama reload:
```jsx
it('click en Reintentar llama reload', async () => {
  const reload = vi.fn()
  useDashboard.mockReturnValue({ ...MOCK_DASHBOARD_STATE, loading: false, error: 'Error', reload })
  renderPage()
  await userEvent.click(screen.getByText('Reintentar'))
  expect(reload).toHaveBeenCalled()
})
```

Para verificar TeacherCard renderizado: Con `teachers` conteniendo 1 profesor "Samuel", verificar `screen.getByText('Samuel')`.

Descripciones en espanol. Mantener bajo 150 lineas.
  </action>
  <verify>
    <automated>npx vitest run src/tests/DashboardPage.test.jsx</automated>
  </verify>
  <acceptance_criteria>
    - src/tests/DashboardPage.test.jsx existe y tiene >= 6 tests
    - Contiene `vi.mock('../hooks/useDashboard.js'` con default mock
    - Contiene `useDashboard.mockReturnValue(` con objeto mock completo
    - Contiene test de estado loading (DashboardSkeleton)
    - Contiene test de estado error con texto 'Error de red' o similar
    - Contiene `getByText('Reintentar')` o `getByRole('button', { name: /Reintentar/ })`
    - Contiene test que verifica reload() llamado tras click en Reintentar
    - Contiene test que verifica texto 'Dashboard' en estado normal
    - Contiene test que verifica dato numerico de totalStudents (ej: '48')
    - `npx vitest run src/tests/DashboardPage.test.jsx` pasa con 0 failures
    - Archivo tiene menos de 250 lineas
  </acceptance_criteria>
  <done>DashboardPage tiene tests de integracion que verifican estados de carga, error y datos, incluyendo boton Reintentar y renderizado de TeacherCards.</done>
</task>

<task type="auto">
  <name>Task 3: Verificacion de cobertura >= 60%</name>
  <files>src/tests/Avatar.test.jsx, src/tests/ProgressBar.test.jsx, src/tests/ToggleSwitch.test.jsx, src/tests/SearchInput.test.jsx</files>
  <read_first>
    - vite.config.js (verificar que thresholds estan configurados correctamente)
    - package.json (verificar script test:coverage existe)
  </read_first>
  <action>
Ejecutar `npm run test:coverage` y verificar que:
1. Todos los tests pasan (89 existentes + todos los nuevos de Plans 01, 02, 03)
2. La cobertura total supera el 60% en lines, functions, branches y statements
3. El comando sale con codigo 0 (thresholds satisfechos)

Si la cobertura NO alcanza el 60% (posible, depende del baseline):
- Revisar el reporte HTML en `coverage/index.html` para identificar que archivos tienen baja cobertura
- Per D-04: agregar tests minimos para componentes simples no cubiertos (Avatar, ProgressBar, ToggleSwitch, SearchInput) SOLO si es necesario para llegar al 60%
- Cada test de componente simple debe ser 1-2 tests basicos: renderiza con props, aplica className
- Archivos candidatos (creados solo si necesario per D-04):
  - src/tests/Avatar.test.jsx
  - src/tests/ProgressBar.test.jsx
  - src/tests/ToggleSwitch.test.jsx
  - src/tests/SearchInput.test.jsx

Si la cobertura YA supera el 60% sin tests adicionales: marcar tarea como completada sin crear archivos.

CRITICO: `npm run test:coverage` debe salir con codigo 0. Si sale con codigo != 0 por threshold, hay que agregar mas tests hasta que pase.
  </action>
  <verify>
    <automated>npm run test:coverage</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:coverage` sale con codigo 0 (exit code 0)
    - La salida muestra lines >= 60%, functions >= 60%, branches >= 60%, statements >= 60%
    - Todos los tests pasan sin failures
    - Si se agregaron tests de componentes simples, cada archivo tiene menos de 250 lineas
  </acceptance_criteria>
  <done>La cobertura total del proyecto es >= 60% verificada automaticamente por los thresholds de Vitest. El comando npm run test:coverage pasa limpio.</done>
</task>

</tasks>

<verification>
- `npm run test:coverage` sale con codigo 0
- Cobertura >= 60% en las 4 metricas (lines, functions, branches, statements)
- AttendancePage y DashboardPage tienen tests que cubren flujos criticos
- Todos los tests (existentes + nuevos) pasan
- `npm run lint` pasa sin errores
</verification>

<success_criteria>
- AttendancePage tiene >= 5 tests de integracion pasando
- DashboardPage tiene >= 6 tests de integracion pasando
- `npm run test:coverage` pasa con codigo 0 (cobertura >= 60% enforzada)
- El reporte de cobertura muestra todas las metricas >= 60%
- Cero tests rotos (todos los existentes + nuevos pasan)
</success_criteria>

<output>
After completion, create `.planning/phases/05-cobertura-de-tests/05-03-SUMMARY.md`
</output>
