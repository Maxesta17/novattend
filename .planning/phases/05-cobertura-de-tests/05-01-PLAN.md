---
phase: 05-cobertura-de-tests
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.js
  - src/tests/buildTeachersHierarchy.test.js
  - src/tests/useStudents.test.jsx
autonomous: true
requirements: [TEST-01, TEST-04]
must_haves:
  truths:
    - "npm run test:coverage ejecuta vitest con proveedor V8 y genera reporte"
    - "El build falla si cobertura baja del 60% (thresholds enforzados)"
    - "buildTeachersHierarchy transforma datos planos en jerarquia correctamente"
    - "useStudents carga alumnos mock, toggleStudent alterna estado, toggleAll marca/desmarca todos"
  artifacts:
    - path: "package.json"
      provides: "Script test:coverage y devDep @vitest/coverage-v8"
      contains: "test:coverage"
    - path: "vite.config.js"
      provides: "Configuracion de cobertura V8 con thresholds 60%"
      contains: "thresholds"
    - path: "src/tests/buildTeachersHierarchy.test.js"
      provides: "Tests unitarios de la funcion pura de transformacion"
      min_lines: 30
    - path: "src/tests/useStudents.test.jsx"
      provides: "Tests unitarios del hook de carga de alumnos"
      min_lines: 40
  key_links:
    - from: "vite.config.js"
      to: "@vitest/coverage-v8"
      via: "provider: 'v8'"
      pattern: "provider.*v8"
    - from: "vite.config.js"
      to: "threshold enforcement"
      via: "test.coverage.thresholds"
      pattern: "thresholds.*lines.*60"
---

<objective>
Instalar la infraestructura de cobertura V8 en Vitest y escribir tests unitarios para la logica core de datos: la funcion pura buildTeachersHierarchy y el hook useStudents.

Purpose: Establece la base de cobertura (infrastructure per TEST-01) y protege la logica de datos mas critica (per TEST-04) antes de escribir tests de paginas y componentes ARIA.
Output: package.json con devDep y script, vite.config.js con coverage config, 2 archivos de test nuevos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cobertura-de-tests/05-CONTEXT.md
@.planning/phases/05-cobertura-de-tests/05-RESEARCH.md
@.planning/codebase/TESTING.md

<interfaces>
<!-- Fuente de verdad: src/utils/buildTeachersHierarchy.js -->
export default function buildTeachersHierarchy(profesores, resumen)
// profesores: [{ id: 'prof-1', nombre: 'Samuel' }]
// resumen: [{ profesor_id: 'prof-1', grupo: 'G1', alumno_id: 'a1', nombre: 'Ana', semanal: 90, quincenal: 85, mensual: 88 }]
// returns: [{ id, name, initial, groups: [{ id, number, students: [{ id, name, weekly, biweekly, monthly }] }] }]

<!-- Fuente de verdad: src/hooks/useStudents.js -->
export default function useStudents(convocatoria, profesorId)
// returns: { students, loadingStudents, loadError, selectedGroup, setSelectedGroup, toggleStudent, toggleAll, presentCount, absentCount, attendancePercent }
export { GROUPS } // ['G1', 'G2', 'G3', 'G4']

<!-- Dependencias de useStudents que se deben mockear -->
import { isApiEnabled } from '../config/api'
import { getAlumnos } from '../services/api'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Infraestructura de cobertura V8</name>
  <files>package.json, vite.config.js</files>
  <read_first>
    - package.json (ver scripts y devDependencies actuales)
    - vite.config.js (ver estructura de config test existente)
  </read_first>
  <action>
1. Instalar la dependencia exacta:
   ```bash
   npm install --save-dev @vitest/coverage-v8@4.0.18
   ```
   CRITICO: version 4.0.18 exacta — la latest (4.1.2) tiene peer dep conflict con vitest 4.0.18 instalado. Verificar con `npm ls @vitest/coverage-v8` que no hay warnings de peer dep.

2. Agregar script en package.json bajo "scripts":
   ```json
   "test:coverage": "vitest run --coverage"
   ```
   No modificar los scripts existentes ("test", "test:watch"). El script "test:coverage" es adicional per D-07.

3. Agregar bloque `coverage` dentro de la clave `test` existente en vite.config.js. La clave `test` ya tiene `environment`, `globals`, `setupFiles`. Agregar `coverage` como propiedad hermana:
   ```js
   test: {
     environment: 'jsdom',
     globals: true,
     setupFiles: './src/tests/setup.js',
     coverage: {
       provider: 'v8',
       reporter: ['text', 'html'],
       include: ['src/**/*.{js,jsx}'],
       exclude: [
         'src/tests/**',
         'src/main.jsx',
         'src/App.jsx',
       ],
       thresholds: {
         lines: 60,
         functions: 60,
         branches: 60,
         statements: 60,
       },
     },
   },
   ```
   Per D-08: thresholds DEBEN estar anidados bajo `test.coverage.thresholds` — no en `test.thresholds` (Vitest los ignora silenciosamente si estan mal anidados).

4. Verificar que `npm run test:coverage` ejecuta sin error (aunque la cobertura actual este bajo 60%, el comando debe funcionar y mostrar el reporte).
  </action>
  <verify>
    <automated>npm run test:coverage 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - package.json contiene `"@vitest/coverage-v8": "4.0.18"` en devDependencies (version exacta, no caret)
    - package.json contiene `"test:coverage": "vitest run --coverage"` en scripts
    - vite.config.js contiene `provider: 'v8'` dentro de `test.coverage`
    - vite.config.js contiene `thresholds:` con `lines: 60, functions: 60, branches: 60, statements: 60` dentro de `test.coverage`
    - vite.config.js contiene `include: ['src/**/*.{js,jsx}']` dentro de `test.coverage`
    - vite.config.js contiene `exclude:` con `'src/tests/**'` dentro de `test.coverage`
    - `npm ls @vitest/coverage-v8` muestra version 4.0.18 sin peer dep warnings
    - `npm run test:coverage` ejecuta y muestra tabla de cobertura en stdout (puede fallar por threshold, pero ejecuta)
  </acceptance_criteria>
  <done>El proveedor de cobertura V8 esta instalado, el script test:coverage funciona, y los thresholds de 60% estan configurados en vite.config.js.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tests unitarios de buildTeachersHierarchy</name>
  <files>src/tests/buildTeachersHierarchy.test.js</files>
  <read_first>
    - src/utils/buildTeachersHierarchy.js (implementacion completa de la funcion)
    - src/tests/api.test.jsx (patron de test sin rendering — referencia de estructura)
  </read_first>
  <behavior>
    - Test 1: Un profesor con un grupo y un alumno produce la estructura correcta (name, initial, groups.length, students[0].monthly)
    - Test 2: Dos profesores con multiples grupos producen la jerarquia completa
    - Test 3: Profesores sin registros en resumen producen profesor con groups vacio
    - Test 4: Array vacio de profesores retorna array vacio
    - Test 5: Campos faltantes (semanal/quincenal/mensual null) usan fallback 0 via ??
    - Test 6: El campo `number` extrae el digito de 'G1' correctamente (Number('1') = 1)
  </behavior>
  <action>
Crear `src/tests/buildTeachersHierarchy.test.js` (extension .js, no .jsx — no hay JSX).

La funcion es pura: import directo, datos fixture inline, sin mocks.

Estructura del archivo:
```js
import { describe, it, expect } from 'vitest'
import buildTeachersHierarchy from '../utils/buildTeachersHierarchy.js'
```

Fixtures de datos inline en cada describe/it (no archivo externo):
- PROFESORES: `[{ id: 'prof-1', nombre: 'Samuel' }, { id: 'prof-2', nombre: 'Maria' }]`
- RESUMEN con campos: `{ profesor_id, grupo, alumno_id, nombre, semanal, quincenal, mensual }`

Tests en espanol: descripciones como `'mapea un profesor con un grupo y un alumno'`, `'retorna array vacio si profesores es vacio'`, etc.

Aserciones clave:
- `result[0].name` === `'Samuel'` (mapea nombre)
- `result[0].initial` === `'S'` (primera letra mayuscula)
- `result[0].groups[0].number` === `1` (extrae numero de 'G1')
- `result[0].groups[0].students[0].monthly` === valor del resumen
- Campos null: `result[0].groups[0].students[0].weekly` === `0` (fallback ??)

Mantener archivo bajo 100 lineas — funcion simple, tests concisos.
  </action>
  <verify>
    <automated>npx vitest run src/tests/buildTeachersHierarchy.test.js</automated>
  </verify>
  <acceptance_criteria>
    - src/tests/buildTeachersHierarchy.test.js existe y tiene >= 6 tests
    - El archivo importa `from '../utils/buildTeachersHierarchy.js'`
    - Contiene test para array vacio: `expect(buildTeachersHierarchy([], [])).toEqual([])`
    - Contiene asercion de `result[0].initial` (verifica extraccion de inicial)
    - Contiene asercion de `.monthly` o `.weekly` con valor numerico
    - Contiene asercion de campo null/undefined con fallback a 0
    - `npx vitest run src/tests/buildTeachersHierarchy.test.js` pasa con 0 failures
    - Archivo tiene menos de 250 lineas (regla CLAUDE.md)
  </acceptance_criteria>
  <done>buildTeachersHierarchy tiene tests unitarios que cubren transformacion basica, multiples profesores/grupos, edge cases vacios, y fallbacks de campos null.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tests unitarios de useStudents hook</name>
  <files>src/tests/useStudents.test.jsx</files>
  <read_first>
    - src/hooks/useStudents.js (implementacion completa del hook)
    - src/tests/useDashboard.test.jsx (patron existente de renderHook en el proyecto)
    - src/tests/LoginPage.test.jsx (patron de mock de servicios API)
  </read_first>
  <behavior>
    - Test 1: En modo sin API (isApiEnabled=false), carga alumnos mock del grupo G1 y loadingStudents es false
    - Test 2: toggleStudent alterna el campo present de un alumno por indice
    - Test 3: toggleAll marca todos como presentes si ninguno lo esta
    - Test 4: toggleAll desmarca todos si todos estan presentes
    - Test 5: presentCount, absentCount y attendancePercent reflejan el estado actual
    - Test 6: GROUPS exporta ['G1', 'G2', 'G3', 'G4']
  </behavior>
  <action>
Crear `src/tests/useStudents.test.jsx` (extension .jsx por renderHook).

Patron de mocking (per D-05 y D-06 — mock a nivel de servicio API):
```jsx
vi.mock('../config/api', () => ({ isApiEnabled: vi.fn(() => false) }))
vi.mock('../services/api', () => ({ getAlumnos: vi.fn() }))
```

Imports despues del mock:
```jsx
import { isApiEnabled } from '../config/api'
```

Usar `renderHook` de `@testing-library/react` (ya disponible, patron de useDashboard.test.jsx).

CRITICO: Usar `await waitFor(() => expect(result.current.loadingStudents).toBe(false))` despues de cada renderHook para evitar act() warnings. El hook tiene useEffect async que actualiza estado.

Para testear toggleStudent y toggleAll, usar `act()` de `@testing-library/react`:
```jsx
import { renderHook, waitFor, act } from '@testing-library/react'

act(() => { result.current.toggleStudent(0) })
```

El hook en modo sin API carga MOCK_GROUPS.G1 = 12 nombres. Verificar:
- `result.current.students.length` === 12
- `result.current.students[0].name` === `'Laura Garcia'` (primer nombre del mock)
- `result.current.students[0].present` === `false` (estado inicial)

Despues de toggleStudent(0):
- `result.current.students[0].present` === `true`
- `result.current.presentCount` === 1
- `result.current.absentCount` === 11
- `result.current.attendancePercent` === 8 (Math.round(1/12*100))

Para GROUPS export:
```jsx
import useStudents, { GROUPS } from '../hooks/useStudents.js'
expect(GROUPS).toEqual(['G1', 'G2', 'G3', 'G4'])
```

Descripciones en espanol. beforeEach con vi.clearAllMocks() e isApiEnabled.mockReturnValue(false).
Mantener archivo bajo 150 lineas.
  </action>
  <verify>
    <automated>npx vitest run src/tests/useStudents.test.jsx</automated>
  </verify>
  <acceptance_criteria>
    - src/tests/useStudents.test.jsx existe y tiene >= 6 tests
    - El archivo importa `from '../hooks/useStudents.js'`
    - Contiene `vi.mock('../config/api'` y `vi.mock('../services/api'`
    - Contiene `renderHook` y `waitFor` de @testing-library/react
    - Contiene test que verifica `students.length` > 0 tras carga mock
    - Contiene test que verifica `toggleStudent` alterna `present`
    - Contiene test que verifica `toggleAll`
    - Contiene test que verifica `GROUPS` exportado
    - `npx vitest run src/tests/useStudents.test.jsx` pasa con 0 failures
    - Archivo tiene menos de 250 lineas (regla CLAUDE.md)
  </acceptance_criteria>
  <done>useStudents tiene tests unitarios que cubren carga mock, toggle individual, toggle all, estadisticas derivadas y constante GROUPS.</done>
</task>

</tasks>

<verification>
- `npm test` pasa todos los tests existentes (89) + los nuevos
- `npm run test:coverage` ejecuta y genera reporte de cobertura V8
- Los 2 test files nuevos pasan independientemente
- `npm run lint` pasa sin errores
</verification>

<success_criteria>
- @vitest/coverage-v8@4.0.18 instalado como devDependency
- vite.config.js tiene coverage con provider v8, thresholds 60%, include/exclude
- package.json tiene script test:coverage
- buildTeachersHierarchy tiene >= 6 tests pasando
- useStudents tiene >= 6 tests pasando
- npm test incluye los tests nuevos sin romper los 89 existentes
</success_criteria>

<output>
After completion, create `.planning/phases/05-cobertura-de-tests/05-01-SUMMARY.md`
</output>
