---
phase: 02-rendimiento-y-bundle
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/DashboardPage.jsx
  - src/components/features/StudentRow.jsx
  - src/components/features/TeacherCard.jsx
  - src/components/ui/StatCard.jsx
autonomous: true
requirements: [PERF-02, PERF-03, PERF-05]

must_haves:
  truths:
    - "Escribir en el campo de busqueda del Dashboard no causa re-renders de searchResults en cada keystroke — el debounce de 300ms absorbe los keystrokes rapidos"
    - "getProfesores y getResumen se ejecutan en paralelo con Promise.all (no en waterfall)"
    - "StudentRow, TeacherCard y StatCard no re-renderizan cuando el padre re-renderiza si sus props no cambian"
  artifacts:
    - path: "src/pages/DashboardPage.jsx"
      provides: "Debounce en searchQuery + Promise.all + useCallback para handlers"
      contains: "useDebounce"
    - path: "src/components/features/StudentRow.jsx"
      provides: "Componente envuelto en React.memo"
      contains: "memo("
    - path: "src/components/features/TeacherCard.jsx"
      provides: "Componente envuelto en React.memo"
      contains: "memo("
    - path: "src/components/ui/StatCard.jsx"
      provides: "Componente envuelto en React.memo"
      contains: "memo("
  key_links:
    - from: "src/pages/DashboardPage.jsx"
      to: "searchResults useMemo"
      via: "debouncedSearch en lugar de searchQuery"
      pattern: "debouncedSearch.*allStudents"
    - from: "src/pages/DashboardPage.jsx"
      to: "src/services/api.js"
      via: "Promise.all([getProfesores(), getResumen()])"
      pattern: "Promise\\.all"
---

<objective>
Optimizar rendimiento del Dashboard con memoizacion, debounce y paralelizacion de API calls.

Purpose: Hacer las listas fluidas bajo carga (React.memo evita re-renders innecesarios), eliminar lag en busqueda (debounce 300ms), y reducir tiempo de carga del Dashboard (Promise.all para API calls paralelas).

Output: 3 componentes con React.memo, DashboardPage con useDebounce + useCallback + Promise.all.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-rendimiento-y-bundle/02-CONTEXT.md
@.planning/phases/02-rendimiento-y-bundle/02-RESEARCH.md

<interfaces>
<!-- Handlers actuales en DashboardPage que se pasan a componentes memorizados -->

From src/pages/DashboardPage.jsx:
```jsx
// Handlers inline actuales que necesitan useCallback:
onToggle={() => setExpandedTeacher(expandedTeacher === teacher.id ? null : teacher.id)}
onStudentClick={setSelectedStudent}
onClick={() => setShowAlertPopup(true)}
onClear={() => setSearchQuery('')}
onChange={e => setSearchQuery(e.target.value)}
```

From src/components/features/StudentRow.jsx:
```jsx
export default function StudentRow({ name, initials, isPresent, onToggle, delay, stats })
```

From src/components/features/TeacherCard.jsx:
```jsx
export default function TeacherCard({ teacher, isExpanded, onToggle, onStudentClick })
```

From src/components/ui/StatCard.jsx:
```jsx
export default function StatCard({ icon, value, label, color, variant, className, onClick })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: React.memo en StudentRow, TeacherCard y StatCard</name>
  <files>src/components/features/StudentRow.jsx, src/components/features/TeacherCard.jsx, src/components/ui/StatCard.jsx</files>
  <read_first>
    - src/components/features/StudentRow.jsx (componente actual — export default function)
    - src/components/features/TeacherCard.jsx (componente actual con sub-componentes GroupSection y ChevronIcon)
    - src/components/ui/StatCard.jsx (componente actual — export default function)
  </read_first>
  <action>
    **1. StudentRow.jsx** — Envolver en memo:
    - Agregar `import { memo } from 'react'` al inicio
    - Cambiar `export default function StudentRow(` a `export default memo(function StudentRow(`
    - Agregar `)` de cierre de memo al final del componente (despues del cierre de la funcion)
    - No cambiar NADA del cuerpo del componente ni de sus props
    - El JSDoc existente se mantiene

    **2. TeacherCard.jsx** — Envolver en memo:
    - Agregar `memo` al import existente: `import { useState, memo } from 'react'`
    - Cambiar `export default function TeacherCard(` a `export default memo(function TeacherCard(`
    - Agregar `)` de cierre de memo al final del componente TeacherCard (linea 70 actual, antes de `function GroupSection`)
    - NO envolver GroupSection ni ChevronIcon en memo — son componentes internos que se benefician del memo del padre
    - No cambiar NADA del cuerpo de TeacherCard

    **3. StatCard.jsx** — Envolver en memo:
    - Agregar `import { memo } from 'react'` al inicio
    - Cambiar `export default function StatCard(` a `export default memo(function StatCard(`
    - Agregar `)` de cierre de memo al final del componente
    - No cambiar NADA del cuerpo del componente
  </action>
  <verify>
    <automated>npm test && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - src/components/features/StudentRow.jsx contiene `import { memo } from 'react'`
    - src/components/features/StudentRow.jsx contiene `export default memo(function StudentRow(`
    - src/components/features/TeacherCard.jsx contiene `import { useState, memo } from 'react'`
    - src/components/features/TeacherCard.jsx contiene `export default memo(function TeacherCard(`
    - src/components/ui/StatCard.jsx contiene `import { memo } from 'react'`
    - src/components/ui/StatCard.jsx contiene `export default memo(function StatCard(`
    - npm test sale con exit code 0 (los tests existentes de StudentRow, StatCard siguen pasando)
    - npm run lint sale con exit code 0
  </acceptance_criteria>
  <done>StudentRow, TeacherCard y StatCard envueltos en React.memo. Tests existentes pasan sin cambios — memo no altera la interfaz externa del componente.</done>
</task>

<task type="auto">
  <name>Task 2: Debounce, useCallback y Promise.all en DashboardPage</name>
  <files>src/pages/DashboardPage.jsx</files>
  <read_first>
    - src/pages/DashboardPage.jsx (estado actual completo — 272 lineas)
    - .planning/phases/02-rendimiento-y-bundle/02-RESEARCH.md (patrones useDebounce, Promise.all, useCallback)
  </read_first>
  <action>
    **Cambios en DashboardPage.jsx (3 optimizaciones en un archivo):**

    **A. Agregar useCallback y useRef a los imports de React:**
    Cambiar linea 1 de:
    `import { useState, useMemo, useEffect } from 'react'`
    a:
    `import { useState, useMemo, useEffect, useCallback, useRef } from 'react'`

    **B. Agregar hook useDebounce inline (antes de la funcion DashboardPage):**
    ```jsx
    function useDebounce(value, delay) {
      const [debouncedValue, setDebouncedValue] = useState(value)
      useEffect(() => {
        const timer = setTimeout(() => setDebouncedValue(value), delay)
        return () => clearTimeout(timer)
      }, [value, delay])
      return debouncedValue
    }
    ```

    **C. Agregar debouncedSearch y cambiar searchResults:**
    Despues de `const [showAlertPopup, setShowAlertPopup] = useState(false)` agregar:
    ```jsx
    const debouncedSearch = useDebounce(searchQuery, 300)
    ```

    Cambiar searchResults (lineas 130-133) de:
    ```jsx
    const searchResults = useMemo(() => {
      if (searchQuery.length < 2) return []
      return allStudents.filter(s => s.name.toLowerCase().includes(searchQuery.toLowerCase()))
    }, [searchQuery, allStudents])
    ```
    a:
    ```jsx
    const searchResults = useMemo(() => {
      if (debouncedSearch.length < 2) return []
      return allStudents.filter(s => s.name.toLowerCase().includes(debouncedSearch.toLowerCase()))
    }, [debouncedSearch, allStudents])
    ```
    IMPORTANTE: El `<SearchInput value={searchQuery}` NO cambia — el campo sigue usando searchQuery directo para feedback inmediato al usuario. Solo el CALCULO de resultados usa debouncedSearch.

    **D. Estabilizar handlers con useCallback para componentes memorizados:**
    Despues de los useState y antes del loadConvData, agregar:
    ```jsx
    const handleAlertClick = useCallback(() => setShowAlertPopup(true), [])
    const handleAlertClose = useCallback(() => setShowAlertPopup(false), [])
    const handleStudentClose = useCallback(() => setSelectedStudent(null), [])
    const handleClear = useCallback(() => setSearchQuery(''), [])
    const handleTeacherToggle = useCallback((id) => {
      setExpandedTeacher(prev => prev === id ? null : id)
    }, [])
    ```

    Actualizar usos en JSX:
    - Linea 204 (`onClick={() => setShowAlertPopup(true)`) cambia a `onClick={handleAlertClick}`
    - Linea 216 (`onClear={() => setSearchQuery('')}`) cambia a `onClear={handleClear}`
    - Linea 249 (`onToggle={() => setExpandedTeacher(...)`) cambia a `onToggle={() => handleTeacherToggle(teacher.id)}`
    - Linea 250 (`onStudentClick={setSelectedStudent}`) se mantiene — setSelectedStudent ya es estable
    - Linea 261 (`onClose={() => setSelectedStudent(null)}`) cambia a `onClose={handleStudentClose}`
    - Linea 267 (`onClose={() => setShowAlertPopup(false)}`) cambia a `onClose={handleAlertClose}`

    **E. Promise.all — getProfesores en paralelo con getResumen (PERF-05):**
    La funcion `loadConvData` en lineas 37-43 YA usa Promise.all:
    ```jsx
    const loadConvData = async (conv) => {
      const [profesores, resumen] = await Promise.all([
        getProfesores(),
        getResumen(conv.id),
      ])
      setTeachers(buildTeachersHierarchy(profesores || [], resumen || []))
    }
    ```
    Esta funcion ya cumple PERF-05. NO hace falta cambiarla. Solo verificar que existe tal cual.

    **F. Verificar que DashboardPage no excede 250 lineas:**
    Los cambios agregan ~15 lineas (useDebounce hook + useCallbacks). El archivo actual tiene 272 lineas. Total estimado: ~287 lineas — EXCEDE 250.

    Solucion: Extraer `useDebounce` a `src/hooks/useDebounce.js`:
    ```jsx
    // src/hooks/useDebounce.js
    import { useState, useEffect } from 'react'

    /**
     * Hook de debounce. Retrasa la actualizacion del valor.
     * @param {*} value - Valor a debounce-ar
     * @param {number} delay - Milisegundos de retraso
     * @returns {*} Valor debounced
     */
    export default function useDebounce(value, delay) {
      const [debouncedValue, setDebouncedValue] = useState(value)
      useEffect(() => {
        const timer = setTimeout(() => setDebouncedValue(value), delay)
        return () => clearTimeout(timer)
      }, [value, delay])
      return debouncedValue
    }
    ```

    Y en DashboardPage agregar:
    `import useDebounce from '../hooks/useDebounce.js'`

    Esto evita que DashboardPage supere el limite de 250 lineas de CLAUDE.md. Si tras todos los cambios el archivo sigue por debajo de 250, el hook puede quedarse inline — Claude tiene discrecion.
  </action>
  <verify>
    <automated>npm test && npm run lint && node -e "const f=require('fs').readFileSync('src/pages/DashboardPage.jsx','utf8'); const n=f.split('\n').length; console.log('Lines:', n); process.exit(n > 250 ? 1 : 0)"</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/DashboardPage.jsx contiene `useCallback` en los imports de React
    - src/pages/DashboardPage.jsx contiene `useDebounce(searchQuery, 300)` o `useDebounce(searchQuery, 300)` via import
    - src/pages/DashboardPage.jsx contiene `debouncedSearch` en el useMemo de searchResults
    - src/pages/DashboardPage.jsx contiene `handleAlertClick` definido con useCallback
    - src/pages/DashboardPage.jsx contiene `handleTeacherToggle` definido con useCallback
    - src/pages/DashboardPage.jsx contiene `Promise.all` en loadConvData (ya existente)
    - src/pages/DashboardPage.jsx tiene 250 lineas o menos
    - npm test sale con exit code 0
    - npm run lint sale con exit code 0
  </acceptance_criteria>
  <done>DashboardPage tiene debounce en busqueda (300ms), handlers estabilizados con useCallback, Promise.all en API calls, y no excede 250 lineas.</done>
</task>

</tasks>

<verification>
- `npm test` — todos los tests pasan
- `npm run lint` — sin errores
- `grep "memo(" src/components/features/StudentRow.jsx` — encuentra memo wrapper
- `grep "memo(" src/components/features/TeacherCard.jsx` — encuentra memo wrapper
- `grep "memo(" src/components/ui/StatCard.jsx` — encuentra memo wrapper
- `grep "debouncedSearch" src/pages/DashboardPage.jsx` — encuentra el debounce
- `grep "useCallback" src/pages/DashboardPage.jsx` — encuentra los handlers estabilizados
- `grep "Promise.all" src/pages/DashboardPage.jsx` — confirma paralelizacion
- DashboardPage.jsx tiene <= 250 lineas
</verification>

<success_criteria>
- 3 componentes envueltos en React.memo
- DashboardPage usa debounce de 300ms en busqueda (silencioso, per D-08)
- DashboardPage tiene handlers con useCallback para componentes memorizados
- Promise.all para getProfesores + getResumen confirmado
- DashboardPage <= 250 lineas (compliance CLAUDE.md)
- Todos los tests pasan sin cambios
</success_criteria>

<output>
After completion, create `.planning/phases/02-rendimiento-y-bundle/02-03-SUMMARY.md`
</output>
