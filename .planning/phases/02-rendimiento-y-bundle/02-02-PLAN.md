---
phase: 02-rendimiento-y-bundle
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - vite.config.js
  - src/App.jsx
  - src/components/ui/LoadingSpinner.jsx
  - src/tests/LoadingSpinner.test.jsx
autonomous: true
requirements: [PERF-01, PERF-04]

must_haves:
  truths:
    - "El chunk inicial (LoginPage) no incluye codigo de ConvocatoriaPage, AttendancePage, SavedPage ni DashboardPage"
    - "Al navegar a una ruta lazy, aparece un spinner branded mientras carga el chunk"
    - "El build genera archivos vendor-react y vendor-router separados en dist/assets/"
    - "LoginPage y NotFoundPage siguen como imports estaticos — sin spinner al cargar"
  artifacts:
    - path: "vite.config.js"
      provides: "manualChunks config para vendor splitting"
      contains: "vendor-react"
    - path: "src/App.jsx"
      provides: "React.lazy imports + Suspense wrapper"
      contains: "React.lazy"
    - path: "src/components/ui/LoadingSpinner.jsx"
      provides: "Spinner branded para Suspense fallback"
      exports: ["default"]
    - path: "src/tests/LoadingSpinner.test.jsx"
      provides: "Tests de LoadingSpinner"
      min_lines: 10
  key_links:
    - from: "src/App.jsx"
      to: "src/components/ui/LoadingSpinner.jsx"
      via: "Suspense fallback prop"
      pattern: "fallback=.*LoadingSpinner"
    - from: "src/App.jsx"
      to: "src/pages/ConvocatoriaPage.jsx"
      via: "React.lazy dynamic import"
      pattern: "lazy.*import.*ConvocatoriaPage"
---

<objective>
Implementar code-splitting por ruta y vendor chunk splitting.

Purpose: Reducir el bundle inicial ~50% para teachers. Las 4 paginas post-login se cargan bajo demanda. React y React Router se separan en chunks cacheables entre deploys.

Output: App.jsx con React.lazy + Suspense, LoadingSpinner.jsx nuevo, vite.config.js con manualChunks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-rendimiento-y-bundle/02-CONTEXT.md
@.planning/phases/02-rendimiento-y-bundle/02-UI-SPEC.md
@.planning/phases/02-rendimiento-y-bundle/02-01-SUMMARY.md

<interfaces>
<!-- Estado de vite.config.js tras Plan 01 (registerType ya es 'prompt') -->
<!-- Solo se agrega manualChunks en build.rollupOptions.output -->

From src/App.jsx (estado actual):
```jsx
import LoginPage from './pages/LoginPage'
import ConvocatoriaPage from './pages/ConvocatoriaPage'
import AttendancePage from './pages/AttendancePage'
import SavedPage from './pages/SavedPage'
import DashboardPage from './pages/DashboardPage'
import NotFoundPage from './pages/NotFoundPage'
// <Routes> sin Suspense wrapper
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear LoadingSpinner.jsx y sus tests</name>
  <files>src/components/ui/LoadingSpinner.jsx, src/tests/LoadingSpinner.test.jsx</files>
  <read_first>
    - .planning/phases/02-rendimiento-y-bundle/02-UI-SPEC.md (contrato visual del spinner)
    - src/components/ui/ErrorBanner.jsx (patron de componente ui/ puro)
  </read_first>
  <behavior>
    - Test 1: Renderiza el texto "Cargando..."
    - Test 2: Renderiza un elemento SVG (el spinner)
    - Test 3: Tiene un contenedor con clase min-h-screen
  </behavior>
  <action>
    Crear test file `src/tests/LoadingSpinner.test.jsx` con los 3 tests descritos. Ejecutar — deben fallar (RED).

    Crear `src/components/ui/LoadingSpinner.jsx`:
    - Componente puro sin props (o con props opcionales para reutilizacion futura)
    - Contenedor: `min-h-screen bg-dark-bg flex flex-col items-center justify-center gap-3`
    - SVG spinner de 32x32px:
      - `<svg className="animate-spin" width="32" height="32" viewBox="0 0 32 32">`
      - Circulo base completo: `<circle cx="16" cy="16" r="14" fill="none" strokeWidth="3" className="stroke-burgundy/30" />`
      - Arco animado (75% del circulo): `<circle cx="16" cy="16" r="14" fill="none" strokeWidth="3" className="stroke-gold" strokeLinecap="round" strokeDasharray="66 22" />`
      - strokeDasharray: 66 es ~75% de la circunferencia (2 * pi * 14 ~ 88), 22 es el gap
    - Texto: `<p className="text-white/80 text-xs font-montserrat font-semibold">Cargando...</p>`
    - JSDoc header describiendo el componente
    - Opcionalmente aceptar `[animation-duration:0.8s]` en el SVG para spin mas rapido (Claude's discretion — animate-spin default tambien es aceptable)

    Ejecutar tests — deben pasar (GREEN).
  </action>
  <verify>
    <automated>npm test -- --testPathPattern LoadingSpinner</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ui/LoadingSpinner.jsx existe y exporta default function LoadingSpinner
    - src/tests/LoadingSpinner.test.jsx existe con al menos 3 tests
    - LoadingSpinner.jsx contiene texto "Cargando..."
    - LoadingSpinner.jsx contiene `<svg` con `animate-spin`
    - LoadingSpinner.jsx contiene `min-h-screen` y `bg-dark-bg`
    - LoadingSpinner.jsx contiene `stroke-gold` y `stroke-burgundy/30`
    - LoadingSpinner.jsx NO contiene ningun `style={{`
    - npm test -- --testPathPattern LoadingSpinner sale con exit code 0
  </acceptance_criteria>
  <done>LoadingSpinner renderiza spinner SVG branded con texto "Cargando...", cumple UI-SPEC, tests pasan.</done>
</task>

<task type="auto">
  <name>Task 2: Code-splitting en App.jsx + manualChunks en vite.config.js</name>
  <files>src/App.jsx, vite.config.js</files>
  <read_first>
    - src/App.jsx (imports actuales — 4 paginas estaticas que deben ser lazy)
    - vite.config.js (tras Plan 01 — registerType ya es 'prompt', no tiene build section)
    - .planning/phases/02-rendimiento-y-bundle/02-RESEARCH.md (patrones React.lazy y manualChunks)
  </read_first>
  <action>
    **1. src/App.jsx** — Cambiar 4 imports a React.lazy y envolver Routes en Suspense:

    Estado objetivo completo:
    ```jsx
    import { lazy, Suspense } from 'react'
    import { BrowserRouter, Routes, Route } from 'react-router-dom'
    import './App.css'
    import MobileContainer from './components/MobileContainer'
    import ProtectedRoute from './components/ProtectedRoute'
    import LoadingSpinner from './components/ui/LoadingSpinner.jsx'
    // Eager (sin cambio):
    import LoginPage from './pages/LoginPage'
    import NotFoundPage from './pages/NotFoundPage'
    // Lazy (per D-05):
    const ConvocatoriaPage = lazy(() => import('./pages/ConvocatoriaPage'))
    const AttendancePage = lazy(() => import('./pages/AttendancePage'))
    const SavedPage = lazy(() => import('./pages/SavedPage'))
    const DashboardPage = lazy(() => import('./pages/DashboardPage'))

    function App() {
      return (
        <BrowserRouter>
          <MobileContainer>
            <Suspense fallback={<LoadingSpinner />}>
              <Routes>
                <Route path="/" element={<LoginPage />} />
                <Route path="/convocatorias" element={
                  <ProtectedRoute allowedRole="teacher"><ConvocatoriaPage /></ProtectedRoute>
                } />
                <Route path="/attendance" element={
                  <ProtectedRoute allowedRole="teacher"><AttendancePage /></ProtectedRoute>
                } />
                <Route path="/saved" element={
                  <ProtectedRoute allowedRole="teacher"><SavedPage /></ProtectedRoute>
                } />
                <Route path="/dashboard" element={
                  <ProtectedRoute allowedRole="ceo"><DashboardPage /></ProtectedRoute>
                } />
                <Route path="*" element={<NotFoundPage />} />
              </Routes>
            </Suspense>
          </MobileContainer>
        </BrowserRouter>
      )
    }
    export default App
    ```

    Notas:
    - UN solo `<Suspense>` envolviendo `<Routes>` completo, no por cada ruta
    - LoginPage y NotFoundPage siguen como import estatico (per D-05)
    - `import { lazy, Suspense } from 'react'` al inicio

    **2. vite.config.js** — Agregar seccion build con manualChunks:

    Agregar despues de la seccion `plugins: [...]` (antes del cierre de `defineConfig`):
    ```js
    build: {
      rollupOptions: {
        output: {
          manualChunks: {
            'vendor-react': ['react', 'react-dom'],
            'vendor-router': ['react-router-dom'],
          }
        }
      }
    }
    ```

    La estructura final de vite.config.js sera:
    ```js
    export default defineConfig({
      test: { ... },
      plugins: [ ... ],
      build: {
        rollupOptions: {
          output: {
            manualChunks: {
              'vendor-react': ['react', 'react-dom'],
              'vendor-router': ['react-router-dom'],
            }
          }
        }
      }
    })
    ```
  </action>
  <verify>
    <automated>npm run build && ls dist/assets/ | grep vendor && npm run lint && npm test</automated>
  </verify>
  <acceptance_criteria>
    - src/App.jsx contiene `import { lazy, Suspense } from 'react'`
    - src/App.jsx contiene `const ConvocatoriaPage = lazy(() => import('./pages/ConvocatoriaPage'))`
    - src/App.jsx contiene `const AttendancePage = lazy(() => import('./pages/AttendancePage'))`
    - src/App.jsx contiene `const SavedPage = lazy(() => import('./pages/SavedPage'))`
    - src/App.jsx contiene `const DashboardPage = lazy(() => import('./pages/DashboardPage'))`
    - src/App.jsx contiene `<Suspense fallback={<LoadingSpinner />}>`
    - src/App.jsx contiene `import LoginPage from './pages/LoginPage'` (eager, no lazy)
    - src/App.jsx contiene `import NotFoundPage from './pages/NotFoundPage'` (eager, no lazy)
    - vite.config.js contiene `'vendor-react': ['react', 'react-dom']`
    - vite.config.js contiene `'vendor-router': ['react-router-dom']`
    - `npm run build` produce archivos con "vendor-react" y "vendor-router" en dist/assets/
    - npm run lint sale con exit code 0
    - npm test sale con exit code 0
  </acceptance_criteria>
  <done>4 rutas post-login son lazy-loaded, LoginPage/NotFoundPage son eager, build genera chunks vendor-react y vendor-router separados, todos los tests y lint pasan.</done>
</task>

</tasks>

<verification>
- `npm run build` — exitoso, genera chunks separados
- `ls dist/assets/ | grep vendor` — muestra vendor-react y vendor-router
- `npm test` — todos los tests pasan
- `npm run lint` — sin errores
- `grep "lazy" src/App.jsx` — encuentra los 4 lazy imports
</verification>

<success_criteria>
- Build genera al menos 6 chunks: vendor-react, vendor-router, index (LoginPage), + 4 page chunks
- LoginPage y NotFoundPage estan en el chunk principal (no lazy)
- LoadingSpinner aparece como fallback de Suspense
- Todos los tests pasan incluyendo los nuevos de LoadingSpinner
</success_criteria>

<output>
After completion, create `.planning/phases/02-rendimiento-y-bundle/02-02-SUMMARY.md`
</output>
